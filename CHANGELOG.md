# Development Log

## 2026-09-19 - Prepare the personal repository update

- Align README and linked architecture documents with the implemented Linux boundary, unavailable public A2 lease activation, in-memory Context Fabric and limited Control Hub relay. Include the original ColdHao donation image at the end of README.
- Build before source tests in `pnpm verify`, because MCP resource tests read the generated UI assets on a fresh checkout.
- Recover from concurrent first-start configuration migration only when another caller has published a valid configuration; keep rejecting a backup collision without a valid replacement. Add a deterministic filesystem-interleaving regression alongside the existing two-process test.

## 2026-09-19 - Stop approval loops after logical Chat session drift

- Symptom: After a user approved an exact non-Codex operation, the first retry could arrive under a rotated `openai/session` and return a fresh `OWNER_APPROVAL_REQUIRED`. The follow-up instruction described claim recovery only in abstract session-drift terms, so a host/model could surface the duplicate approval instead of reclaiming the already-approved receipt.
- Changes: Strengthen the approval-card follow-up contract: retry the exact approved operation first; if that retry returns a new approval requirement, do not ask the user to approve again. Claim the original approval receipt using `claim_approval` (or the cached-host `review_approval("__claim__<original approvalId>")` compatibility path), retry the original exact operation once, and stop if claim fails. No Owner approval, OAuth, execution-boundary or lease scope was broadened.
- Verification: The follow-up regression was first made RED on the missing `OWNER_APPROVAL_REQUIRED` branch, then passed 2/2 after the instruction fix. A live compatibility claim of an already-approved `run_process` receipt then allowed the exact retry to execute without generating another approval; the combined regression/build batch passed 62/62 and produced a Candidate whose manifest fingerprint matched current source.

## 2026-09-19 - Default A2 Candidate execution to a verified no-network profile

- Risk: After credential containment v3, A2 Candidate execution still inherited the host network. That left a long-lived workspace lease able to reach arbitrary network destinations even though the design contract specified no general network capability by default.
- Changes: Bump the Linux source boundary profile to `linux-bwrap-workspace-rw-host-ipc-masked-env-filtered-net-profile-v4` and add explicit `inherit | none` network profiles. A2 Candidate plans now use `networkProfile="none"`; `exec_command`, `bash` and `run_process` propagate that profile through `ProcessSessionManager` to Bubblewrap, where `none` adds `--unshare-net`. Legacy one-shot Owner-approved project execution keeps inherited networking. Process snapshots and Candidate execution evidence expose the effective network profile.
- Lease compatibility: Raise `A2_WORKSPACE_LEASE_POLICY_VERSION` to `a2-workspace-lease-v2-network-none`. Recovery therefore invalidates older policy-version leases instead of silently reinterpreting them under the new network semantics.
- Verification: Boundary self-test now requests the no-network profile and compares the child and parent `/proc/self/ns/net` identities; a missing/different-profile failure is fail-closed. Targeted regression passed 46/46 with one expected nested-Bubblewrap environment skip, and `pnpm typecheck` exited 0. Full `pnpm test` passed 396 tests with 381 passed, 0 failed and 15 platform/environment skips in about 149.5 seconds.
- Deployment status: Source is verified at v4, but the active DevSpace connector used for this verification still reported `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2`. No production rebuild/restart/promotion is claimed here; runtime v4 activation must be verified after controlled deployment.
- Remaining: A verified allowlisted egress profile is not implemented. RCF Verify/Hold/Quarantine/Freeze, guarded Candidate promotion/rollback and restart-durable Candidate state remain separate unresolved A2 work.

## 2026-09-19 - Add labeled Decision Benchmark Harness (L1)

- Changes: Extend Decision Intelligence benchmarking from single-call latency/token capture to labeled provider evaluation. Add exact accuracy, normalized Brier score, expected calibration error, explicitly labeled False-Safe/False-Alarm rates, Score mean absolute error, mean/p95 latency, token totals and optional caller-supplied cost estimates. Add multi-provider execution that returns independent reports without selecting a winner.
- Evidence discipline: Ground truth is fixture-owned, safety metrics are computed only when fixtures explicitly define unsafe Noul values, Choice labels or Score thresholds, missing answers/type drift fail closed, and pricing is caller-supplied rather than embedded in the control plane.
- Authority boundary: Benchmark reports remain evidence only. They cannot approve, execute, promote a provider, or override deterministic RCF/A2/Owner controls.
- Verification: New benchmark harness regression passed 3/3 and `pnpm typecheck` exited 0 after the implementation. Existing Decision Intelligence and wider repository regression remain separate verification scopes.

## 2026-09-19 - Contain inherited host credentials inside the A2 project boundary

- Risk: Bounded project shell/native execution previously inherited the complete DevSpace service environment. Filesystem credential masks protected common on-disk locations, but service-level API keys, tokens, passwords, agent sockets and container/Kubernetes control endpoints could still cross the project-process boundary through environment variables.
- Changes: Add boundary-owned environment filtering and bump the Linux source profile to `linux-bwrap-workspace-rw-host-ipc-masked-net-inherit-env-filtered-v3`. The v3 boundary removes common token/key/password/private-key patterns plus explicit SSH/GPG/D-Bus/Docker/Kubernetes/cloud credential/control variables, while preserving ordinary development variables such as PATH/HOME/LANG. Expand credential-directory masks to Azure, Kubernetes, Docker, GitHub/GLab, rclone and 1Password config locations. `host_command` remains the separate Owner-gated host-maintenance plane and does not inherit this project-boundary filter.
- Verification: Targeted boundary/verifier/process/readiness/startup/MCP authorization regression passed 38/38 with one expected nested-Bubblewrap environment skip, and `pnpm typecheck` exited 0. Full `pnpm test` then passed 391 tests with 376 passed, 0 failed and 15 platform/environment skips in about 148 seconds. Boundary verification now explicitly checks that sensitive environment filtering is present.
- Deployment status: Source is verified at v3, but the active DevSpace connector used for this verification still reported `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2`. No production rebuild/restart/promotion is claimed by this entry; runtime v3 activation must be verified after controlled deployment.
- Remaining: Network is still inherited and needs an explicit egress policy. RCF Verify/Hold/Quarantine/Freeze plus guarded Candidate promotion/rollback and restart-durable Candidate state remain separate unresolved A2 work.

## 2026-09-19 - Restore full regression after A2 boundary and project-access convergence

- Symptom: The first full regression after the A2/Workspace Lease rollout reported 10 failures. The failures mixed real project-access cache drift with stale migration/startup expectations and test fixtures that attempted to nest Bubblewrap inside the already-isolated DevSpace execution boundary.
- Changes: Align OAuth/startup tests with schema migration v8 and the additional `WorkspaceLeaseStore` database handle; give the npm package-privacy fixture a workspace-local npm cache; classify the runner's nested user-namespace failure as an unavailable Pi sandbox environment; add bounded validated inventory/text-body caches to project access; and keep MCP reconnect/cancellation fixtures focused on lifecycle semantics by disabling their redundant inner execution boundary.
- Cache safety: Inventory caching is bounded to 8 complete catalogs of at most 20,000 files and revalidates Git candidate signatures plus observed directory metadata. Text caching is bounded to 32 entries / 32 MiB and revalidates canonical path plus inode/size/mtime/ctime metadata before reuse. Existing stale-cursor, SHA, symlink and root-containment checks remain authoritative.
- Verification: L1 targeted regression passed 15/15 with one expected Pi-sandbox environment skip; package privacy passed 1/1; project-access regression passed 7/7 and TypeScript typecheck exited 0; MCP reconnect/cancellation regression passed 4/4. The final `pnpm test` run passed with 390 tests, 375 passed, 0 failed and 15 platform/environment skips in about 148 seconds.
- Release verification: `pnpm build` exited 0, `pnpm test:deploy` passed 19/19, and `pnpm test:docs` passed 5/5 with 36 documents / 112 local links / 0 documentation errors. The converged source/build/test chain is green. Vite still reports several chunks above 500 kB; this remains performance debt, not a correctness failure.
- Performance baseline: `pnpm benchmark:project-access` measured 291 eligible files across 7 inventory rounds and 15 text-read rounds. Validated inventory cache reuse reduced median latency from 15.645 ms to 11.845 ms (1.321x), while validated text cache reuse on `src/server.ts` (66,624 bytes) reduced median latency from 0.992 ms to 0.474 ms (2.093x). Diagnostics observed exactly one inventory build / one inventory cache hit and one text-body read / one text cache hit per cold-warm pair; benchmark assertions preserved snapshot and SHA-256 consistency. `pnpm typecheck` exited 0 after the benchmark.

## 2026-09-19 - Keep candidate pnpm state inside the A2 workspace boundary

- Symptom: `deploy.mjs --candidate-only --yes` failed before compilation with pnpm `ERR_SQLITE_ERROR: unable to open database file` because pnpm attempted to open its host-level store/index outside the workspace-write execution boundary.
- Changes: Candidate preparation now uses a dedicated `.runtime/pnpm-store` via `--store-dir` for both direct pnpm and npm-fallback invocations. The store directory is created under the existing ignored runtime area and symlink stores are rejected before dependency preparation.
- Verification: Deployment regression now asserts that candidate builds pass the workspace-local store path to pnpm. A real candidate-only benchmark is required after this patch to confirm the host-global SQLite failure is removed and to measure build time.

## 2026-09-19 - Add bounded local Control Hub relay client v0.1

- Changes: Add a fixed-action local Control Hub client and shared `control_hub` MCP tool for public health, node hello, this node's coordination permissions and unread notifications. Runtime configuration is environment-only (`DEVSPACE_CONTROL_HUB_URL`, node ID/client ID/token and timeout); no Cloudflare secret is added to durable DevSpace config or model-visible output.
- Safety: The client has no arbitrary URL/path/body action, does not upload Chat/Codex IO yet, and advertises only the actually implemented node capability (`status`). Cloud coordination remains non-authoritative for local execution. HTTP errors are bounded and never include the configured bearer token.
- Scope: Automatic Context IO offload is intentionally deferred until a local redaction/denylist gate exists; callers are not trusted merely because a cloud payload has `redacted:true`.
- Verification: Targeted Control Hub client, server tool-surface, Owner authorization and TypeScript checks are required after this patch before runtime promotion is claimed.

## 2026-09-19 - Add advisory TypeSafe Decision Intelligence adapter (L1)

- Changes: Add a provider-neutral Decision Intelligence contract, TypeSafe System One HTTP adapter, credential redaction helper, A/B observation builders for agent traces and mutation risk, and a small benchmark wrapper for latency/provider token telemetry. The adapter uses the documented /v1/systemone endpoint with jev-latest by default, accepts the documented structured EntryType criteria/legends, validates Noul/Choice/Score responses, and uses SDK-aligned retry classes for connection/timeout failures plus 408/429/5xx.
- Safety: The integration is advisory only. It has no approval, shell, filesystem, lease, promotion or break-glass capability; RCF/A2/Owner policy remains authoritative. The API key is constructor-only and is not added to durable DevSpace config, Context Fabric, Cloudflare state, logs or errors. The credential destination is fixed to api.typesafe.ai and outbound state/questions receive automatic key-based plus bounded inline-secret redaction with redacted-path telemetry.
- Verification: Baseline Context Fabric regression passed 5/5 before the change. TypeSafe adapter tests and full typecheck are required after this patch before the integration is considered verified. A live API call is intentionally not part of the source test because no secret-bearing tool argument should be recorded.

## 2026-09-19 - Add Context Fabric runtime consumer v0.1 (candidate verified)

- Runtime: Add one shared `context_fabric` MCP tool with `put_anchor`, `append_delta` and `capsule` actions. Its service-level `ContextFabricStore` is workspace-scoped and survives MCP session replacement while the process lives; restart clears it. Anchor replacement is explicit, Delta sequence is monotonic, and per-record/workspace limits bound memory use.
- Authority: Context Fabric validates the existing Anchor/Delta/Budget contracts but cannot grant execution authority, access project files or expand workspace roots. The Owner authorization classifier treats valid Context Fabric operations as bounded routine state operations; malformed inputs fail validation instead of entering approval.
- Verification: Context Fabric/server/Owner regression passed 37/37 with zero failures; TypeScript typecheck exited 0. A fresh isolated candidate build succeeded under `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2` and contains both `context-fabric.js` and `context-fabric-tools.js`. Direct execution of the compiled candidate Store completed `putAnchor -> appendDelta -> capsule`, superseding `old` with `new` and returning only evidence `b`.
- Deployment: Production promotion is not claimed. The active Chat tool schema still lacks the host-maintenance surface needed to identify/restart the host supervisor safely, so the verified candidate remains isolated until a controlled promotion path is available.

## 2026-09-19 - Route eligible A2 execution through Candidate Workspace

- Changes: Add `CandidateExecutionCoordinator` and connect it to Workspace Lease runtime authorization plus Codex `exec_command`, Claude `bash`, native `run_process`, and their long-session continuation paths. ACTIVE persisted authority becomes `executionEligible=true` only when the boundary is explicitly verified and the Candidate provider probe succeeds; the coordinator then consumes one short-lived private grant, remaps `cwd/workspaceRoot` to Candidate state and returns bounded mutation evidence.
- Safety: Stable Workspace is never the writable execution target on the A2-ready path. Candidate grants are single-use and memory-only, long-running sessions retain the same Candidate until completion/cancel, and mutation path lists are capped while preserving counts/changed-bytes/Stable-drift evidence. Production still defaults `workspaceLeaseBoundaryVerified=false`, so no lease execution is enabled without an explicit trusted verification signal. No automatic promotion was added.
- Verification: Coordinator/lease/server integration regression passed 32/32 with zero failures; real OAuth/MCP authorization regression passed 11/11, including both fail-closed `boundary_unverified` behavior and the verified A2 Candidate path that bypasses per-operation Owner approval while leaving Stable unchanged. Full TypeScript typecheck exited 0 after the final OAuth/MCP test addition.
- Remaining: Replace the injected boundary-verification flag with a trusted startup/runtime self-test; make Candidate recovery durable across restart if promotion is to survive process loss; add RCF Verify/Hold/Quarantine/Freeze and guarded promotion/rollback; define the final network policy before describing A2 as complete.

## 2026-09-19 - Add Candidate Workspace Provider v0.1

- Changes: Add a Linux filesystem Candidate Workspace provider that creates candidate storage outside Stable Workspace, snapshots regular files with reflink-if-supported / copy fallback semantics, rewrites internal symlinks to Candidate-local targets, tracks a verified baseline manifest, reports created/modified/deleted paths plus changed bytes and Stable drift, and only discards provider-owned candidates.
- Safety: Hardlinks are not used. Broken or workspace-escaping symlinks and special filesystem entries fail closed. Snapshot creation verifies Stable before/after copy and verifies the Candidate manifest before registration. The provider is not connected to project execution yet, so Workspace Lease remains `executionEligible=false`.
- Verification: Candidate Provider regression passed 6/6 with zero failures; full TypeScript typecheck exited 0 under `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2`.
- Remaining: Candidate Execution Coordinator must route shell/native execution into Candidate state, preserve session lifecycle for long-running processes, emit mutation evidence, and keep Stable unchanged until later RCF verification/promotion.

## 2026-09-19 - Wire persisted Workspace Lease into runtime authorization (fail-closed)

- Changes: Add `WorkspaceLeaseRuntime` and context lookup in `WorkspaceLeaseStore`; the server now owns the lease store lifecycle and observes matching persisted A2 authority for `exec_command`, `bash` and `run_process` using OAuth client, logical conversation, canonical workspace, policy version and boundary profile.
- Safety: This cut does not grant lease execution. An ACTIVE persisted lease is reported as authority-only with `executionEligible=false` while Candidate Workspace is unavailable, so project execution still follows the existing exact Owner-approval path. Lease integrity failure blocks execution; unverified boundary recovery suspends the lease; profile/policy mismatch invalidates it.
- Verification: Workspace Lease store/runtime plus real OAuth/MCP authorization regression passed 20/20 with zero failures. Server/process/boundary lifecycle regression passed 28/32 with zero failures and four nested-Bubblewrap environment skips. Full TypeScript typecheck exited 0 under `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2`.
- Remaining: Candidate Workspace, mutation observation, RCF Verify/Promote/Hold/Quarantine/Freeze and a verified network policy are still required before an ACTIVE workspace lease may authorize mutation-capable execution without a per-operation Owner approval.

## 2026-09-19 - Validate Context Fabric candidate and long-history folding trial (L1)

- Candidate: `node scripts/deploy.mjs --candidate-only --yes` completed successfully and produced an isolated verified build under `.runtime`; the running `dist`, configuration, credentials and tunnel were not replaced or restarted.
- Trial: The compiled candidate `control-plane/context-fabric.js` ran two direct experiments. A small mixed-state sample folded 120 estimated tokens to 60 (2.00x). A longer supersession-heavy sample with 181 historical statements folded 2302 estimated tokens to 142 (16.21x), leaving 21 current statements after supersession and materializing 11 under the configured budget.
- Boundary: The live execution profile observed during the later trial was `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2`. A project-shell supervisor probe could see only its Bubblewrap namespace, confirming that host process/systemd inspection must use the separate Owner-gated `host_command` plane rather than bypassing IPC isolation.
- Status/Risk: Production promotion is intentionally not claimed. The current Chat tool schema does not expose `host_command`, so the actual host supervisor cannot be safely identified or restarted from this conversation. Context Fabric also has no runtime consumer yet; promoting the library alone would not reduce Chat context automatically.

## 2026-09-18 - Close host-control socket escape in A2 Boundary v2

- Evidence: A live Phase 1 probe from sandboxed `run_process` successfully executed `systemd-run --user --wait --collect --quiet /bin/true`. A read-only host filesystem therefore did not constitute a closed workspace boundary because Unix-domain sockets can exercise host authority without mutating their filesystem inode.
- Fix: Bump the Linux profile to `linux-bwrap-workspace-rw-host-ipc-masked-net-env-inherit-v2`; hide `/run/user/$UID` inside project execution and mask common Docker/containerd/Podman, system D-Bus and systemd private control sockets. Resolver state remains visible so this fix does not silently redefine the existing inherited-network contract.
- Safety: This is capability isolation, not command-text filtering. `host_command` remains the explicit Owner-gated recovery plane. Phase 2 promotion is blocked until the live `systemd-run --user` probe fails under v2.

## 2026-09-18 - Extend A2 workspace boundary to project shells (Phase 2)

- Changes: Route Codex `exec_command` pipe/PTY sessions and Claude `bash` synchronous commands through the same configured `WorkspaceExecutionBoundary` used by native `run_process`. Preserve `write_stdin`, PTY resize/SIGINT behavior, the pre-A2 non-PTY POSIX `-c` contract, and Claude's bounded timeout contract through `ProcessSessionManager`; remove the redundant Pi shell executor.
- Authorization: Propagate the effective runtime boundary profile into initial classification, Approval Center context revalidation and pre-dispatch TOCTOU revalidation. Approval text now reflects the actual server instance instead of inferring isolation from `process.platform`.
- Verification: Targeted Phase 2/native/authorization/server regression passed 52/57 with zero failures and five environment-specific skips; four skips are nested Bubblewrap namespace tests because the regression command itself ran inside the deployed Phase 1 boundary, and one is Windows-only. Full TypeScript typecheck exited 0 through boundary profile `linux-bwrap-workspace-rw-net-env-inherit-v1`.
- Safety/Scope: `host_command` remains the explicit unsandboxed recovery plane. The Linux project boundary still inherits network and process environment. Candidate workspace, mutation verification, Workspace Lease integration and Phase 2 runtime promotion are separate gates.

## 2026-09-18 - Deploy A2 native workspace execution boundary (Phase 1)

- Changes: Add Linux Bubblewrap profile `linux-bwrap-workspace-rw-net-env-inherit-v1` for native `run_process`: host root read-only, selected workspace writable, normal production `/tmp` private, common credential locations masked, workspace `.env` / `.env.local` read-only, and PID/UTS/IPC isolated.
- Verification: A2/native/authorization/server regression passed with zero failures; TypeScript typecheck and candidate build passed. Managed promotion returned ready, and the live runtime probe wrote inside the workspace while a sibling write failed with `EROFS`; the response reported the expected boundary profile.
- Remaining boundary: Network and inherited environment are intentionally not isolated by this profile. `host_command` remains the separate Owner-gated recovery plane.

## 2026-09-18 - Add Pattern Folding Context Fabric v0.1 (L1)

- Changes: Add `src/control-plane/context-fabric.ts` with explicit ContextAnchor, ContextDelta, EvidenceRef, ContextCapsule and ContextBudget contracts. Folding uses Anchor + Delta + Exception + References, supports explicit supersession of stale state, and selects optional context by bounded priority/role ordering while preserving required statements.
- Safety: Factual folded statements require evidence references; missing referenced evidence, cross-anchor deltas and required-context budget overflow fail closed. Token estimates are caller-supplied and are not presented as tokenizer measurements. The module has no execution, approval, Cloudflare or prompt-authority capability.
- Verification: Context Fabric + existing Control Plane + Relay Contract tests passed 13/13 with zero failures. `pnpm typecheck` exited 0. Both commands ran through the observed Linux Bubblewrap boundary profile `linux-bwrap-workspace-rw-net-env-inherit-v1`.
- Scope: This cut establishes the local folding contract only. It does not yet persist capsules, perform retrieval, assemble ChatGPT prompts, or integrate a Cloudflare Control Hub.

## 2026-09-18 - Freeze the A2 workspace execution-boundary design (design only)

- Current State: `exec_command` / `bash` and native `run_process` still execute with the DevSpace service account authority; current conversation leases are exact-scope, memory-only grants tied to the approval lifetime. The repository has no direct Bubblewrap backend, but the Pi adapter already uses `@anthropic-ai/sandbox-runtime` with workspace-write, symlink-containment and protected-file tests.
- Decision: Define A2 as a separate workspace lease + RCF guard + verified OS execution boundary + candidate workspace + mutation verification + controlled promotion pipeline. Split `ApprovalRequestTTL` from 4h/8h/12h/24h `WorkspaceLeaseTTL`; persist only a signed recovery envelope and durable incident state; require recovery self-tests; keep break-glass single-use and outside the lease.
- Safety: The document explicitly forbids silent fallback to an unconfined shell, command-text review as a sandbox substitute, filesystem watcher completeness as promotion evidence, candidate-command replay against stable state, and relabelling current conversation leases as A2.
- Scope: Documentation only. No production authorization, shell/process behavior, database schema, UI authority or sandbox claim changed in this cut. Implementation remains split into Workspace Lease, minimal UI/outbox, Execution Boundary + Candidate Workspace, and RCF Incident/Mutation/Promote/Rollback.

## 2026-09-18 - Reduce MCP App template fetch failures under ChatGPT session churn (L1)

- Symptom: ChatGPT could display `Failed to fetch template` even though the v7 app resource passed authenticated `resources/read`, its JS/CSS assets returned HTTP 200 with cross-origin headers, and the main MCP endpoint remained healthy.
- Evidence: Production logs repeatedly showed MCP sessions being closed with `reason:"capacity"` while `openai-mcp/1.0.0` was rapidly creating new sessions. The registry default was 32 sessions, and capacity eviction removes the oldest idle session even if the remote host may still reference it for a later UI-resource read.
- Changes: Raise the default MCP session capacity from 32 to 128 and log successful workspace-app resource reads with the safe resource URI/current-alias flag and HTML byte count. This does not change OAuth, approval authority, workspace access, or tool capabilities.
- Verification: Add a regression proving the default registry no longer evicts at 33 sessions. Existing authenticated MCP resource-read coverage continues to validate the v7 URI, MCP App MIME type, HTML asset URL and CSP metadata.

## 2026-09-18 - Preserve Owner fallback when Chat approval UI fails to load (L1)

- Symptom: A valid Chat approval request could be created and `review_approval` could return successfully while the host displayed a generic card loading error. The private app metadata still contained the Owner URL, but the model-visible review result did not, so the conversation had no recoverable approval path.
- Root Cause: The approval UI treated the MCP App card as the only presentation path after review, even though the server already had a hardened Owner page intended as the fallback.
- Changes: Include the exact Owner `approvalUrl` in single-review summaries and expose pending `fallbackApprovals` from the centralized Approval Center. The decision token remains private and no approval authority changes.
- Verification: Added contract assertions that single-card and center responses expose only the Owner fallback URL while continuing to exclude private decision credentials. Runtime/build deployment remains a separate acceptance gate.

## 2026-09-18 - Serve bounded legacy MCP App resource aliases (L1)

- Symptom: A Chat session could retain an older `review_approval` schema/output-template binding while the running DevSpace service had already advanced its workspace App URI to v7. The approval RPC succeeded, but the host could fail while resolving the cached UI resource and display a generic loading error.
- Root Cause: Resource URI versioning correctly prevented stale UI reuse, but the server removed the previous read-only resource identities immediately. A host that cached the old tool descriptor therefore had no resource to resolve during the reconnect gap.
- Changes: Keep the unversioned workspace App URI and v2-v6 as read-only compatibility aliases that render the current v7 HTML/CSP/assets. New tool definitions still publish only v7, and no decision capability, token, approval scope or legacy authorization behavior is restored.
- Verification: Add MCP resource-list coverage for the bounded compatibility window while retaining the v7-only `review_approval` output-template assertion. Runtime deployment and a real Chat card reload remain separate acceptance gates.

## 2026-09-18 - Repair Chat approval card discovery and continuation (L2)

- Symptom: `review_approval` reached DevSpace and returned a pending approval, but some Chat sessions rendered no inline card; those sessions could also retain an older partial tool surface. After a decision, an explicitly rejected host message could leave Chat waiting for a manual continuation.
- Root Cause: The approval UI contract had accumulated more than one model-visible review entry sharing one output template, while MCP hosts can cache tool/app-resource contracts across reconnects. The follow-up bridge also treated explicit non-delivery the same as ambiguous delivery.
- Changes: Bump the MCP App resource contract to `workspace-app-v7` and app identity to 0.6.0. Use `review_approval` as the single model-facing review entry: omit `approvalId` for the Approval Center and provide it for one request. Keep `review_approvals` app-private for widget compatibility. If the standard message API explicitly rejects delivery, fall back once to the ChatGPT compatibility follow-up; ambiguous delivery still never retries.
- Safety: Approval authority is unchanged. Decision capabilities remain app-private, approval tokens remain outside model-visible content, and follow-up fallback occurs only after explicit non-delivery.

## 2026-09-18 - Add Owner-gated host maintenance command (L2)

- Symptom: When the main DevSpace service or its public ingress needed recovery, Chat had only project-oriented shell/native tools and no explicit host-maintenance contract. A proposed Windows CMD bridge was also invalid for the actual deployment because runtime probing confirmed the DevSpace host is native Ubuntu, not WSL.
- Root Cause: Host maintenance and project execution shared mechanics but lacked a distinct model-facing capability and authorization scope. Treating the Ubuntu host as WSL would have created a false platform contract.
- Changes: Add `host_command` as a shared MCP tool for explicit DevSpace-host maintenance/recovery. It resolves the host shell, executes through the existing native process manager, and reuses timeout, output truncation, `process_status`, `process_cancel`, workspace initial-cwd validation and tool-call auditing. Add an Owner-approval classification plus a principal/root/full-arguments exact conversation lease dedicated to host maintenance.
- Safety: `host_command` is explicitly not sandboxed and does not grant machine-wide standing authority. Conversation reuse requires an explicit user approval and an identical full request; changed command text, arguments, workspace, client or logical conversation fall back to normal approval. No unauthenticated rescue shell or Windows interop claim was added.
- Verification: TDD RED failed 2/15 because `host_command` was absent; after implementation the same targeted suite passed 15/15, typecheck/build passed, and the deployed service restarted onto the new build. The OAuth/HTTP MCP integration now also asserts that authenticated `tools/list` exposes `host_command`; the same full-stack test already verifies that invoking it before Owner approval returns `OWNER_APPROVAL_REQUIRED` with no filesystem side effect.

## 2026-09-18 - Repair Chat approval continuation and control-tool visibility (L2)

- Symptom: Chat approval could reach `approved` while the model tool surface still omitted `claim_approval`, and approval follow-up text always instructed Chat to claim before retrying. This created a deadlock when the host had not actually changed logical session identity.
- Root Cause: Approval-core semantics and host-facing continuation guidance diverged. `OwnerApprovals` already allowed the same OAuth client and same `openai/session` to consume an approved exact retry directly, but `claim_approval`, `reissue_approval` and `revoke_conversation_approvals` lacked explicit model visibility, while UI/server instructions treated claim as unconditional.
- Changes: Mark `claim_approval`, `reissue_approval` and `revoke_conversation_approvals` as model-visible control tools without widget/output-template bindings. Update approval-card and server instructions so the same logical Chat retries the exact approved operation first; `claim_approval` is now the recovery path only when host logical-session rotation prevents direct receipt consumption. Add regressions for control-tool visibility and follow-up ordering.
- Safety: No approval authority is broadened. Claim remains same-OAuth-client only, does not approve or execute an operation, and exact operation/workspace boundaries remain unchanged. UI-only decision/recycle tools remain app-only.
- Verification: `src/mcp-authorization.test.ts`, `src/ui/approval-bridge.test.ts` and `src/ui/tool-result.test.ts` passed 23/23; `pnpm typecheck` and `pnpm build` exited 0. The browser approval-card regression was skipped because no Chromium executable was present in PATH. The built `dist/approval-tools.js` contains three model-visible control-tool registrations; the running MCP process still requires restart/reconnect before Chat can discover the new tool surface.

## 2026-09-17 - Align host-visible approval contracts and exact Shell leases (L2)

- Symptom: The source already contained Approval Center support, but approval-required responses still pointed Chat at the single-card tool, the shared MCP App still identified itself as a diff-only card, and the conversation-approval button did not consistently grant the exact Shell request it described across Codex/Claude tool surfaces. The checked-in `dist` build was also stale relative to source.
- Root Cause: Host-visible metadata, authorization semantics, UI identity and deployment freshness evolved independently. Source tests covered the newer components but did not establish that the running build exposed the same contract.
- Changes: Make `review_approvals` the primary Chat approval entry with `review_approval` retained for one-request recovery; add fixed-TTL, principal-bound, exact-root/exact-arguments Shell leases for both `exec_command` and `bash`; rename the shared MCP resource to `DevSpace Workspace App`; bump the app protocol identity to `devspace-workspace-app` 0.5.0; align authorization documentation with the implemented lease boundary.
- Safety: Exact Shell leases require an explicit user conversation-approval click and an identical complete request. Changed commands, arguments, roots, clients or conversations miss the lease. PTY input, Codex delegation, credentials/service state, destructive file operations and unknown capabilities retain their existing approval boundaries. Leases remain memory-only, fixed-expiry and revocable.
- Verification: Authorization/approval bridge/tool-result regressions passed after the first contract repair. Final build/deployment freshness and real-host tool discovery remain separate acceptance gates and must not be inferred from source-test success.

## 2026-09-17 - Add centralized Approval Center and visible approval lifecycle (L2)

- Symptom: Approval capabilities existed as individual cards, but there was no user-visible queue. Conversation approval, terminal-card collapse and Chat follow-up therefore looked missing or fragmented in the real workflow.
- Changes: Add `review_approvals` with a private-metadata Approval Center that groups pending/in-flight requests, active bounded conversation leases and processed requests. Pending items keep independent decision capabilities; processed items move into a collapsed archive; active leases can be revoked from the center. Individual cards link back to the center and still auto-notify Chat after a decision, with the existing manual recovery action when message delivery is uncertain. Bump the MCP app URI to v3 so hosts cannot reuse the older approval widget contract.
- Safety: The center is a presentation and coordination layer only. It does not add bulk approval, expose decision tokens to model-visible content, broaden Shell/Codex authority, or persist conversation leases across restart.

## 2026-09-17 - Preserve conversation lease lineage and restore Shell single-use safety (L2)

- Symptom: A conversation lease could be handed off across one host `openai/session` rotation, but the approved receipt was deleted when its exact one-shot retry was consumed. A later session rotation therefore lost the lease lineage and produced another approval. The same-day runtime policy had also drifted from the authorization docs by allowing an exact Shell command to become a reusable lease.
- Root Cause: Single-use execution consumption and conversation-lease identity used the same approval record lifecycle. Removing the record was correct for a plain one-shot grant but also removed the only safe handoff anchor for an active conversation lease.
- Changes: Retain a consumed conversation approval as an in-memory lineage record until its fixed lease deadline, revocation or service restart. `claim_approval` can migrate that active lineage repeatedly across host session rotation for the same trusted OAuth client. Restore reusable scopes to safe project review/worktree operations plus an explicitly approved exact `run_process`; Shell remains one-shot even when the user selected the safe-conversation action. Add real `/readyz` startup coverage before deployment.
- Safety: Lineage retention never recreates the consumed one-shot execution grant. Shell/PTY/Codex/destructive or sensitive operations do not gain reusable authority, changed native arguments miss the lease, cross-client claim remains rejected, and revocation deletes consumed lineage along with its leases.
- Verification: Extend authorization regressions through a second session rotation after the original grant is consumed, assert Shell requires a new approval on its second execution, and keep the server startup test gated on `/readyz = 200`.

## 2026-09-17 - Make conversation approval explicit and terminal cards compact (L2)

- Symptom: The backend exposed conversation approval, but the user still saw the previous approval choices and completed cards remained visually expanded.
- Immediate Cause: The MCP app resource kept the same `ui://devspace/workspace-app.html` identity across UI contract changes, allowing a host to reuse an older widget. The card also closed only its parameter `<details>` block instead of collapsing the complete terminal body.
- Root/Architecture Cause: Approval authority, app-resource versioning and terminal presentation were treated as separate implementation details without one end-to-end host-visible contract. The existing lease model also did not provide a useful repeated scope for shell requests.
- Changes: Version the workspace app resource as v2, make conversation leases tool-specific, add an exact-root/exact-arguments shell lease for `exec_command`/`bash`, and collapse approved/denied/submitted cards to a compact terminal view with expandable details. Notification failures reopen the recovery controls automatically.
- Safety: Conversation approval remains user-selected, conversation/principal-bound and fixed-expiry. Shell reuse requires the identical root and complete argument object; changed commands, credentials, Codex submissions, destructive patches and unknown capabilities still require their normal authorization path.
- Verification: Add authorization and browser regressions for scope isolation, exact shell matching, the v2 resource URI, whole-card collapse and recovery expansion. Deployment/runtime acceptance remains separate from source-test success.

## 2026-09-16 - Stabilize conversation approval handoff and always-visible safe lease action (L2)

- Current State: The safe-operation conversation lease existed in source but its button was conditional, and an approved operation could fail to consume when the host changed `openai/session` between the approval-card click and the next Chat turn. That produced a fresh pending approval even though the user had already approved the exact operation.
- Changes: Make the non-Codex approval card consistently expose `本对话内始终同意安全操作`. The action approves the current request once and creates only server-defined safe scopes: project review/worktree permissions plus an exact native-process signature only when that process was explicitly approved. Add `claim_approval` so Chat can bind an already-approved, unconsumed receipt to the current logical session when the same trusted OAuth client receives a new `openai/session`.
- Safety: Claiming never approves a pending/denied/submitted request, never crosses OAuth clients, never executes the operation, and removes only an exact duplicate pending request created by session drift. Shell, Codex, credentials, security configuration, destructive patches and unknown capabilities do not inherit the safe lease.

## 2026-09-16 - Add scoped conversation approval leases (L2)

- Current State: Owner approval was strictly single-use. Repeated aggregate reviews, equivalent worktree creation and identical native test commands required a new click even when the authenticated OAuth client, logical Chat conversation and reviewed scope had not changed.
- Changes: Add an in-memory conversation approval lease created only by the private user decision UI. Leases bind the authenticated client/conversation principal to one server-defined scope and reuse the existing fixed approval deadline without sliding renewal. Eligible scopes are same-workspace `show_changes`, same-source/same-base worktree creation, and an exactly identical `run_process` request. Add `revoke_conversation_approvals` to remove all leases for the current conversation immediately.
- Safety: Shell/PTY input, Codex turns, credentials/service state, security/execution configuration, destructive or large patch operations and unknown capabilities remain single-use. Ordinary guarded small patches already bypass unnecessary consent and therefore do not need a lease. Leases are memory-only, disappear on restart, never cross OAuth clients or `openai/session`, and never expand workspace or sandbox boundaries.

## 2026-09-16 - Introduce Personal Control Plane research contracts and approval relaunch (L2)

- Current State: Routine/complex routing was embedded in the Codex execution module, denied Owner approvals remained pinned until expiry, and recovery concepts were spread across bridge/approval states. Several Eterna research concepts had no explicit engineering boundary, which made it easy either to ignore useful mechanisms or to over-apply names without runtime evidence.
- Changes: Add a deterministic Canonical Intent IR, a permission-neutral Chimera routing module, and ReturnFlow/RCF recovery helpers. Route existing protected Codex auto-selection through those modules without changing the approved model allowlist. Add `reissue_approval`, which can only be used from the same trusted client/conversation after a denial and only arms an exact re-request; it neither approves nor executes the old operation. Codex receipt replay now uses the ReturnFlow recovery rule for uncertain delivery. Document how Shadow/Mirror, Neuron Safety, Eterna layering, Bamboo, Water Intelligence, probability/risk, Dream IR, ColdLang, Zero/Lingxi, Shadow Worlds, Resonance, Pattern Folding and Meta-World map to current or future DevSpace mechanisms.
- Root Cause: Policy, routing and recovery semantics had grown inside provider/security modules instead of a narrow control-plane seam, while denial had no explicit user-driven relaunch transition. Research concepts also lacked a rule distinguishing implemented mechanisms from experimental architecture.
- Safety: `route != authority`, `candidate != live`, `unknown != retryable`. Relaunch preserves the denied record until the exact original operation is requested again and still requires a new human decision. No automatic provider fallback, permission expansion, synthetic risk score, new daemon, event bus or shadow database is introduced.

## 2026-09-16 - Condense task completion reports (L0)

- Changes: Make completion reports follow the user's language, merge overlapping change/fix information, omit empty sections, and allow small tasks to finish in two to four short lines. Collapse accounting to a concise unavailable-source sentence when no real counters exist.
- Reason: The earlier completion contract was accurate but too verbose for routine work and repeated information already visible in the conversation.
- Impact: Workflow/documentation only. Evidence, deployment-state distinctions and accounting requirements remain; presentation is shorter and less repetitive.

## 2026-09-16 - Require evidence-backed task completion summaries (L0)

- Current State: DevSpace already required a traffic/token accounting footer, but the project instructions did not separately require a concise user-facing summary of what work was performed, what was fixed, how it was validated, whether it reached the deployed runtime and what remained unresolved.
- Changes: Add a task-completion reporting contract to `AGENTS.md`, document the standard ChatGPT workflow and compact report shape, and clarify that accounting is the final footer after the task summary rather than a replacement for it.
- Root Cause: Completion reporting and usage accounting were coupled too loosely, so a turn could satisfy the accounting requirement while leaving the actual engineering outcome, fix, validation boundary or deployment state implicit.
- Impact: Workflow/documentation only. Future DevSpace task reports must distinguish outcome, actual changes, fixes, validation evidence, source/build/deploy/runtime state and material remaining issues before the accounting footer.
- Compatibility: No MCP tool schema, runtime behavior, authorization boundary, configuration or code path changes.

## 2026-09-09 - Unblock approval templates without exposing private decisions (L3)

- Current State: The personal branch started at 6370d97 with unrelated accounting edits preserved. A real host probe created an approval and review_approval returned pending, but ChatGPT settings explicitly reported that hidden decide_approval was associated with a template and made that template unavailable.
- Changes: Remove only the output-resource association from the private decide_approval action. Keep review_approval as the rendering entry point. Add real tools/list contract assertions and authorization guidance. Apply a scoped compiled-module hotfix after validating a copy of the existing deployment, then refresh the existing plugin definition in ChatGPT.
- Root Cause: The private click handler incorrectly declared the same ui.resourceUri as the public review/display tool. The host treated that hidden action as a template owner and disabled the shared template. A new regression reproduced this exact metadata relationship before the fix; it is not an Origin, OAuth, directory or user-password failure.
- Impact: Private/app-only visibility, widget accessibility, destructive/open-world annotations, private component capability, client/conversation binding, context checks and one-time consumption remain. No tool was exposed to the model to work around the block. The host's hidden-template warning disappeared after the definition refresh, while the decision tool remained private. A fresh remote request/review succeeded and remained pending; no production decision or test command was executed.
- Tests: Relevant baseline 20 passed; the new metadata assertion failed on the original implementation. Targeted regressions 20 passed. Frozen source suite 254 passed / 7 skipped / 0 failed; typecheck and backend build passed. The copied existing deployment with only the patched module passed 17 authorization/lifetime/login checks; existing built UI passed 4 browser checks. The first compiled test invocation incorrectly included a source-only UI bridge module and failed with ERR_MODULE_NOT_FOUND; that unit remains covered by source tests, and actual bundled UI was checked in a browser. Documentation checked 33 documents and 107 local links with no errors. These scopes overlap and are not additive.
- Compatibility: Tool names, inputs, outputs, OAuth credentials, approval/login lifetimes and permission boundaries are unchanged. Existing UI-only and legacy widget calls remain enabled. Only approval-tools.js was replaced in the running application. Consistent database backups and the original compiled file were retained; configuration/auth hashes and the tunnel process remained unchanged. The former complete-build marker was moved into the private rollback backup because this is a partial hotfix, not deployment of every newer repository change.
- Known Risks: Real host template registration is verified, but the user's approval click and resulting execution were intentionally not performed. Old in-memory approvals expired on restart. The complete source-suite conditional/platform skips remain, and current hosted CI availability must be checked separately. This does not fix a host that omits required conversation context or a genuinely expired/mismatched approval; those requests remain rejected. The running service still combines its earlier base build with this single-file fix.
- Next Highest-Leverage Step: Have the user perform one deliberate decision on a fresh card, verify the resulting receipt and exact-operation behavior, then reconcile the remaining source changes into a separately tested full deployment. Do not reuse old approval links, widen the client allowlist or disable authorization checks.

## 2026-09-09 - Document cost-aware execution and compact accounting (L0)

- Current State: Traffic/token reporting and standing authorization were already implemented and published; the practical cost-control recommendations needed explicit repository guidance.
- Changes: Require search before reading, necessary excerpts and changed context, one primary executor per complex scope, risk-related validation and useful bounded delegation. Collect one accounting snapshot near each turn end, keep the footer compact and retain per-call details in the local report.
- Root Cause: Repeated context, duplicated analysis and repeated accounting refreshes add work without establishing better results or more complete final-response receipts.
- Impact: Project instructions and usage documentation only; all required verification and permission boundaries remain.
- Tests: Two guides passed baseline and post-edit local-link/fence checks; scoped diff and publication-privacy review passed. Shared source guides were synchronized only after matching their prior committed contents. No source logic changed, so code tests were not rerun for this L0 update.
- Compatibility: Existing CLI, token/traffic fields and missing-source semantics are unchanged. Every-turn accounting remains required.
- Known Risks: Guidance does not prove a savings percentage, enforce model behavior automatically, or deploy runtime services. Equal-acceptance task comparisons and actual receipts are still needed.
- Next Highest-Leverage Step: Apply these controls in subsequent work and compare finalized task receipts without adding repeated measurement-only model cycles.

## 2026-09-09 - Roll back failed startup and report real listener readiness (L3)

- Current State: The personal branch started at b6c7317 with unrelated controller-accounting edits. Application construction had no partial-resource rollback. Both launchers used an Express listen callback without checking its error argument, so an occupied port could print readiness and exit successfully. Store constructors performed fallible work after acquiring database handles.
- Changes: Preserve synchronous createServer while rolling back acquired resources in reverse order on construction failure. Add a shared asynchronous startServer path used by both launchers, waiting for a real listening event and closing the application after synchronous or asynchronous bind failure. Move store-specific initialization into openDatabase's existing handle-ownership boundary. Explicitly own and close the SDK's four MemoryStore rate-limit stores. Declare the already-locked express-rate-limit 8.5.2 as a direct dependency, with no dependency graph/version changes. Update lifecycle contracts and tests; preserve the unrelated CLI usage route outside this release.
- Root Cause: Express delivers bind errors to its listen callback, not just successful readiness. Unreturned constructors could not transfer their acquired handles to the caller for cleanup. Default SDK OAuth middleware also created four timers with no owner available to close them. Eight initial regressions failed on the old implementation; the first repair exposed those four additional live timers, which now have explicit ownership. A later positive-test assumption about Node's built-in listening listener was corrected without removing that listener from production code.
- Impact: Failed OAuth, workspace, bridge and later route initialization release acquired connections and timers. Rollback continues after a cleanup error and retains the initial cause. Bind failure is nonzero, never claims readiness, does not choose another port and does not terminate the existing port owner. Store cleanup and successful listener shutdown retain earlier ordering. Resource rollback does not undo committed migrations or reset stored records. Native commands, delegated inference and authentication/approval policy are unchanged.
- Tests: Relevant baseline 40 passed. Initial failing startup suite 8 failed; final related lifecycle/storage regression 38 passed. Frozen source regression 254 passed / 7 skipped / 0 failed in 277.048 seconds. Compiled startup/shutdown contracts 28 passed, built-browser authorization checks 5 passed, and deployment/package/document-checker checks 22 passed. Typecheck and backend/UI builds passed. The document scan checked 33 documents and 107 local links with no errors. Real local HTTP tests retain independent OAuth limits of 100/15min, 50/15min, 20/60min and 50/15min; occupied-port tests cover both actual launchers, and recovery checks retain a fixture record with SQLite integrity_check returning ok. Test scopes overlap and are not additive.
- Compatibility: Existing createServer and one-argument openDatabase callers remain valid; startServer is additive, and only synchronous store setup belongs in its database initializer. No MCP input, schema migration, approval/login expiry, allowed-root or model-policy change. The direct dependency reuses the SDK's existing locked package. Frozen-lockfile validation passed under the available pnpm 11.19 fallback and left the lockfile unchanged; the declared 11.25 executable was not cached. Despite the offline install flag, pnpm supply-chain validation fetched registry metadata. No full fresh dependency installation or production deployment was performed.
- Known Risks: Seven conditional/platform skips, the existing UI chunk warning and hosted CI eligibility remain limitations. Never-settling cleanup, forced-stop deadlines, concurrent first WAL initialization and durable execution recovery are separate work. The tests use temporary state and fixture requests, not a new production ChatGPT account or Codex inference. The isolated CLI candidate excludes parallel accounting code. Full lock resolution under the declared package-manager version and live deployment acceptance remain unverified.
- Next Highest-Leverage Step: Verify a combined, explicitly approved deployment candidate against the running service and host, with source/build receipts and rollback preserved. Design a bounded shutdown policy separately so a hung callback cannot indefinitely delay failure reporting; do not implement that by deleting state or silently killing unrelated processes.

## 2026-09-09 - Complete shutdown cleanup after resource failures (L3)

- Current State: The personal branch started at 3d39ffc with unrelated controller-report, CLI, instruction and log edits. HTTP shutdown awaited application cleanup before observing HTTP failure. Application resource closure stopped at its first thrown error.
- Changes: Observe HTTP and application outcomes together, retaining drainage after a cleanup error. Reuse the existing shutdown module for ordered best-effort application cleanup: timers, MCP sessions, processes, approvals, OAuth storage, workspace storage and bridge resources. Preserve one original error or aggregate multiple causes; reuse the same close Promise on subsequent calls. Add strict child-process, actual-server failure-injection and real HTTP drainage regressions, plus architecture and maintenance guidance.
- Root Cause: Sequential awaits left an early rejected HTTP Promise temporarily unhandled; an application rejection also bypassed the HTTP wait. A straight-line resource cleanup chain skipped later resources when one step threw. The pre-fix new suite reported 10 failures and 2 passes, including strict-process failure and missing later cleanup calls. No global rejection handler or automatic retry was introduced to hide these errors.
- Impact: Both CLI and direct server entry points already use the same shutdown helper; their public invocation contracts remain unchanged. Cleanup order and approval-submission drainage before database closure are preserved. A failure now waits for both sides and retains all reported causes. Attempted cleanup is not proof that a failing resource released successfully. Steady-state tool execution, authentication, model routing and database formats are untouched.
- Tests: Relevant baseline 31 passed. Targeted shutdown/session checks 14 passed; expanded related regressions 36 passed. The frozen source suite completed with 240 passed / 7 skipped / 0 failed in 236.684 seconds. Compiled shutdown contracts 14 passed; built-browser authorization checks 5 passed; deployment/package/document-checker tests 22 passed. Typecheck and backend/UI builds passed, with the existing large-chunk warning retained. Documentation checked 33 documents and 107 local links with no errors. Counts are separate overlapping scopes, not an additive total. Source hashes, snapshot diff and publication privacy were reviewed; detailed receipts remain private.
- Compatibility: No MCP schema, OAuth, approval/login duration, directory boundary, dependency, migration or service configuration changes. Single-error identity and successful shutdown behavior remain; multiple failures now use AggregateError and failure completion waits for HTTP drainage. These internal failure semantics are intentional. Repeated close does not replay cleanup, tasks or approvals. No running service or tunnel was restarted or deployed.
- Known Risks: A never-settling cleanup step or HTTP drain can still block shutdown; no new deadline, forced interruption or process-kill policy was added. Partial startup rollback, concurrent first WAL initialization and durable execution recovery remain separate work. Failure injection uses temporary stores and local child processes, not production failures or a real ChatGPT/Codex task. Conditional/platform skips and remote CI eligibility remain explicit limitations; local success is not hosted CI or deployed acceptance.
- Next Highest-Leverage Step: Define and test ownership and rollback of resources acquired during partial server startup. Handle bind failure and bounded shutdown as separate lifecycle contracts before promoting a combined source/runtime candidate.

## 2026-09-09 - Refresh repository contracts and bound workspace state (L3)

- Current State: The personal branch started at 651434a. Workspace objects and physical-root anchors lived in separate unbounded maps. Public configuration, tool and agent examples lacked automated alignment checks. Concurrent controller-accounting changes were present and excluded from this tested publication.
- Changes: Unified workspace objects and anchors into one cache entry, with a 32-entry LRU for stores supporting durable anchors. Persist anchors before exposing a new cache entry. Added cache safety tests, a documentation checker and CI gate, configuration/example/tool-reference contract tests, a documentation index and maintenance roadmap; refreshed architecture, configuration, security and setup guides. Read-only agent examples now declare writeMode: read_only, and Codex examples use an approved model. Unified Chromium endpoint readiness in the existing test helper.
- Root Cause: Separate maps could diverge during cleanup, and objects accumulated without an eviction contract. Failed anchor persistence could still leave a usable cached workspace. Read-only prose did not enforce profile permissions. Browser regressions reproduced a Windows EBUSY race because file existence was treated as completed endpoint publication; the bounded helper retries only startup reads, not approvals or browser actions.
- Impact: Cache hits update recency; eviction retains persistent identity and the original root anchor. Restoration revalidates that anchor and conservatively resets skill activation as a restart does. Legacy/no-store registries retain instance-local behavior rather than evicting identities they cannot safely restore. Documentation checks cover local targets in root Markdown, docs, examples and skills; they do not validate external availability or every command example. No extra runtime manager or parallel state store was introduced.
- Tests: Selected baseline 38 passed / 3 skipped. Final isolated source regression 227 passed / 7 skipped / 0 failed; explicit browser/login checks 9 passed, compiled cache contracts 6 passed, deployment/package checks 17 passed, and documentation-checker tests 5 passed. The documentation scan checked 33 documents and 107 local links with no errors. Typecheck and backend/UI builds passed. Earlier cache failure evidence and the browser EBUSY failure are retained privately; the corrected browser group passed after the helper fix. The first agent-example test attempt failed because its fixture disabled subagents; that setup error is not product-failure evidence. Reading the original example frontmatter separately confirmed the missing permission fields.
- Compatibility: No public MCP input, OAuth, database schema, configured approval lifetime, login freshness, tunnel or active service changes. Cache eviction may require rereading an advertised skill, as documented. Sample profiles do not replace installed user profiles. No package rename/version bump, dependency upgrade, upstream merge, service deployment or history rewrite. Shared unrelated edits remain outside this release.
- Known Risks: Seven conditional/platform cases remain skipped in the general suite; separate Chromium checks cover the browser cases, not every engine or OS. The existing UI chunk-size warning remains. This is repository/fixture acceptance, not new real ChatGPT account, human approval or Codex inference acceptance. Cache size does not bound all process memory or persistent records. Hosted CI eligibility must be verified separately. Protocol migration, durable command receipts, storage lifecycle, repeated project I/O and duplicated version metadata remain explicitly tracked rather than hidden by a broad rewrite.
- Next Highest-Leverage Step: Reconcile the completed parallel changes into a separately tested deployment candidate, then verify the real host read/guarded-edit/approval path against its recorded build. Design durable execution receipts and restart recovery as a separate compatible change before claiming recoverable jobs.

## 2026-09-09 - Include recorded traffic and standing accounting authorization (L2)

- Current State: The controller report summarized token receipts and visible calls but omitted payload byte counts. Repository guidance still referred to separate authorization for receipt reads despite the user's standing authorization for task accounting.
- Changes: Added recorded-tool-payload traffic totals, per-call request/response bytes, known observed subtotals and explicit wire-traffic unavailability. Updated rules and footer documentation to require traffic and token reporting every turn without renewed approval for related existing receipts; execution permissions remain unchanged.
- Root Cause: Recorded payload size is measurable from the selected JSONL source, but it is not network wire traffic. Pending, absent and unmatched payloads also cannot safely become zero or a complete total.
- Impact: UTF-8 strings and JSON-serialized structured payloads are measured without returning content. Each recorded occurrence, including repeats, contributes bytes while response token receipts remain deduplicated. Missing full-direction totals stay null; observed subtotals include only measured payloads attributable to visible calls. Wire bytes remain null because HTTP/TLS framing, compression, retransmission and model API transfer are not observed.
- Tests: Baseline 10 passed. Reporter plus CLI regression 15 passed, 0 failures/skips, in 35.211 seconds; typecheck and backend compilation passed. Coverage includes multibyte Unicode, structured output, repeated records, missing/pending/unmatched payloads, absent identities and turn boundaries. The compiled CLI returned complete, reconciled token and recorded-payload results for a completed related rollout. Document links, fields, fences and scoped diff checks passed. The prior broader suite was not rerun for this bounded reporter update.
- Compatibility: Additive JSON fields and text lines. No database, model, server permission or network-capture change. Existing token sources and partial/missing semantics remain. Accounting authorization does not authorize unrelated sessions, credentials or execution actions.
- Known Risks: Traffic is recorded tool payload volume, not an ISP/API network invoice. Active snapshots omit later results and final-response usage; subagent and host records must be labeled separately when unavailable. Current runtime services were not deployed by this change.
- Next Highest-Leverage Step: Use the report at each turn boundary, displaying observed traffic subsets and receipt coverage until finalized records are available.

## 2026-09-09 - Add read-only controller usage reports and accounting rules (L2)

- Current State: Delegated Codex thread snapshots were already available, but there was no reusable controller receipt report or repository rule requiring an accounting footer. The release candidate was isolated from the shared checkout.
- Changes: Added `devspace usage report --rollout <path> [--turn-id <id>] [--json]`, its CLI route/help, focused tests, project accounting rules and report documentation. The command reads only an explicitly selected bounded JSONL snapshot, whitelists counters/call metadata and emits no prompts, arguments or tool outputs.
- Root Cause: A response receipt can cover several operations; adding it per tool or adding cumulative snapshots would duplicate usage. Review fixtures also exposed missing thread/session validation and invalid turn-event order being labeled complete; both conditions now prevent complete reports.
- Impact: Canonical response IDs are deduplicated, counter sums reconcile with provider turn totals, and visible outer calls list result presence with independent tokens unavailable. Missing receipts/details, active turns, identity/lifecycle gaps and malformed snapshots are explicit. The reporter does not discover other sessions, infer nested operations, load credentials, start models or contact servers.
- Tests: Related usage baseline 10 passed. Focused reporter/CLI tests 10 passed, including real CLI read-only behavior; independent review reran five identity/lifecycle regressions. Isolated candidate typecheck and backend compilation passed. All 72 discovered source test files completed with 226 passed, 0 failed and 7 conditional skips in 204.287 seconds; skips are four browser opt-ins and three platform cases. The compiled CLI reconciled a completed authorized controller turn against actual structured receipts. Scoped diff, documentation/link and publication-privacy checks passed. A pnpm wrapper initially attempted dependency setup and failed with ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY; the same compiler and discovered test suite were then run directly using existing dependencies, without purging or reinstalling them.
- Compatibility: Additive local CLI command; no database, service, model-policy, permission or existing tool-schema change. Old logs with only token_count produce unavailable rather than zero. Input is capped at 64 MiB and 100,000 JSONL records. Nonblocking open rejects non-regular sources without waiting on a POSIX FIFO.
- Known Risks: Complete refers only to the selected controller turn, not Chat-host, delegated-agent or account billing. Active-turn snapshots exclude later/final-response receipts. Visible batches do not establish each nested operation or its success. POSIX FIFO behavior and opted-out browser/platform cases were not executed on this Windows host. No production service was restarted or deployed; local acceptance is not hosted CI or savings evidence.
- Next Highest-Leverage Step: Use the report at work boundaries with authorized receipt paths, retain explicit missing-source labels, and verify host/delegated receipts independently when measuring complete workflow cost.

## 2026-09-09 - Publish pending usage and OAuth callback updates (L3 changes, scoped release)

- Current State: The personal branch started at 9f8b22e with 22 pending source, test and documentation files. Usage telemetry and a scoped OAuth callback repair had separate earlier validation records; some local notes contained machine/project identifiers.
- Changes: Prepared the pending usage pipeline and OAuth callback fix for separate commits. Removed local project names, paths, exact project-file fingerprints and process identifiers from the pending public log while preserving private evidence. Added the OAuth redirect browser regression to the existing Windows CI browser step; all three CI platforms remain configured.
- Root Cause: Provider usage notifications were not carried through the execution/persistence/presentation chain, and the OAuth form-action policy did not permit its validated cross-origin callback. The new browser test also needed an explicit CI invocation instead of remaining conditionally skipped. These fixes already existed locally and are now released together.
- Impact: Optional provider-thread usage is retained without converting absent telemetry to zero or claiming billing savings. The callback fix permits only the validated current callback origin and keeps the existing OAuth checks. This publication did not restart services, deploy a new dist, alter credentials or change approval/model policies.
- Tests: Current selected baseline 18 passed. Frozen source regression 216 passed, 0 failed, 7 conditional skips; explicit browser/session checks 9 passed and deployment/package checks 17 passed. Typecheck and backend/UI build passed. The workflow parsed successfully, named real test files and retained the three-platform matrix. Scoped privacy scanning and diff review passed. Historical per-feature acceptance is recorded in its dated entries, not treated as new production verification.
- Compatibility: Additive optional usage uses existing execution_json metadata with no database migration. Old records remain readable; older writers may discard telemetry on downgrade. The 30-120 minute approval policy and 12-hour login configuration are not changed by this release. No upstream merge, package version bump, force-push or history rewrite.
- Known Risks: Full-suite conditional/platform skips and the existing UI chunk warning remain. New live model usage, complete host/account billing and long-duration network behavior were not verified by this publication. Hosted CI availability must be checked separately; local tests cannot substitute for an unstarted remote job. Original local diagnosis details are retained only in private evidence.
- Next Highest-Leverage Step: When deployment is requested, reconcile the server and agent daemon on one tested version, then validate observed usage through a user-authorized task. Restore hosted CI eligibility independently of code publication.

## 2026-09-09 - Repair OAuth browser callback blocked by CSP (L3)

- Current State: Public health was reachable, but reauthentication repeatedly returned an authorization 302 while the browser remained on the consent page; the existing connector reported oauth_token_invalid_grant.
- Changes: Allow only the SDK-validated current callback origin alongside self in the OAuth form-action CSP. Preserve all other CSP directives, Owner verification, PKCE, scope/resource checks, expiry and permission gates. Add src/oauth-redirect.browser.test.ts with an isolated real Chromium form submission and token exchange.
- Root Cause: The prior form-action self policy blocked the successful POST/302 redirect to the registered cross-origin OAuth callback. Two pre-fix Chromium runs each observed an issued authorization code, an explicit CSP violation and no callback request. Mock-response tests did not execute browser navigation.
- Impact: Only src/oauth-provider.ts and its compiled dist/oauth-provider.js change product behavior. Only that compiled file was deployed; unrelated shared work was excluded. The managed service restarted while its tunnel and other processes continued.
- Tests: Existing authentication baseline 10/10 passed. New browser regression failed twice before the fix. After the fix, the browser plus existing authentication regressions passed 11/11 with zero skips; callback GET/state/no-Referer, PKCE token exchange HTTP 200 and token verification passed, while an unrelated origin received zero form submissions. Typecheck and staged TypeScript build both exited 0; independent diff review found no blocking issue.
- Live acceptance: The scoped check recorded browser consent returning to host plugin settings, OAuth token exchange HTTP 200 and a complete paginated read matching its local SHA-256. Private project names, paths, file fingerprints and unrelated application details are omitted. These dated observations do not establish long-duration reliability.
- Compatibility: No OAuth database migration, credential reset, permission expansion, node/TUN change or account/session lifetime change. Ordinary reauthentication remains subject to the existing Owner freshness policy.
- Known Risks: Browser navigation regression was run in installed Chromium, not every browser engine. This is scoped OAuth and current TUN coexistence acceptance, not a full repository regression or long-duration network guarantee. The first File.Replace attempt failed on a null backup argument before changing the target; hashes were rechecked before successful replacement with an explicit same-volume backup.
- Recovery: Source/runtime backups, before/after tests, build/typecheck logs and the deployment receipt are retained privately. Machine-specific backup paths and artifact identifiers are not published.
- Next Highest-Leverage Step: Include this browser regression in release checks with DEVSPACE_TEST_BROWSER set; no additional user action is required for the current connection.

## 2026-09-09 - Bounded 30-120 minute approval lifetime (L3)

- Current State: Owner approvals and their browser nonce cookies each used a hardcoded five-minute lifetime, with a separate static page label. Owner login freshness was already configured for 12 hours. Unrelated token-usage work remained in the shared checkout.
- Changes: Added tools.approvalTtlSeconds with a 1800-second default and an integer range of 1800-7200 seconds. Shared limits feed configuration validation and the approval store; the server now passes the configured lifetime. The Owner page displays the exact request expiry and its HttpOnly cookie uses that same absolute deadline. Updated the schema and authorization/configuration guides.
- Root Cause: Approval duration was not a configuration contract, and independent cookie/text constants could disagree with an extended server deadline. Five failing regressions captured missing defaults/bounds, short expiry and cookie mismatch before the implementation.
- Impact: Pending and unconsumed approvals keep the deadline from their first request. Reopening, reviewing, retrying or approving does not extend that deadline. Single-use consumption and principal/context checks remain. Submission continues independently of approval expiry; the existing terminal receipt window now follows the configured lifetime without granting execution authority.
- Tests: Baseline 14 passed; focused checks 22 passed. The isolated release candidate passed 209 source tests with 0 failures and 6 conditional skips; 13 compiled-contract, 8 browser/login and 17 deployment/package checks passed. Tests cover 30/60/120-minute values, out-of-range/non-integer input, exact expiry, no sliding renewal, late single-use consent, cookie/page consistency and ongoing submission. Typecheck, build, schema generation and diff review passed. After promotion, all 389 deployed files matched; effective approval lifetime was 1800 seconds and Owner login remained 43200. Local/public health and OAuth discovery were 200, anonymous MCP was 401.
- Compatibility: Existing configuration files remain valid and need no edit; the Owner-gated default intentionally changes from five to thirty minutes as requested. The public tool inputs, OAuth credentials, allowed roots, model policy and task runtime/other-provider idle timeouts are unchanged. The separate receipt-retention phase is documented. Restart still invalidates in-memory approvals; no persistence format or migration was added.
- Known Risks: Six conditional/platform cases remained skipped; no new real ChatGPT human approval, production command or Codex inference was performed. A longer window retains approval records longer under the existing per-principal/global limits. This is not a blanket grant, automatic renewal or a two-hour task runtime cap. Old links were invalidated by the controlled restart. Existing large UI chunks and the separate concurrent-WAL startup limitation remain.
- Next Highest-Leverage Step: Use a newly created approval in the real host to confirm the displayed deadline and a user decision. Keep optional future lifetime changes within 1800-7200 seconds; do not lengthen OAuth login freshness or remove approval boundaries to work around host reauthentication.

## 2026-09-08 - Unified native tools and migration guard rollout (L3)

- Current State: Published migration guards and the running native-tool build came from different snapshots. The host still exposed an older tool list and required reauthentication. New token-usage work appeared during preparation and was preserved outside this release.
- Changes: Consolidated the existing batch reader, native process/status/cancel tools and explicit context refresh with the published migration-history and legacy-config fixes. Prepared one frozen source snapshot, backed up the current configuration and SQLite state, then promoted its verified dist through the existing supervisor. No permission, login-lifetime, model-policy, tunnel or database-format expansion.
- Root Cause: Source publication and runtime promotion had occurred in separate work streams; a cached host tool list and invalid OAuth grant were independent of backend health. Rebuilding cannot silently restore a host's authorization, so no credentials were reset or approvals bypassed.
- Impact: Runtime now retains the native-tool capabilities while also enforcing migration version/name checks and failed-open cleanup. The fixed 12-hour Owner policy and all six protected configuration files were preserved. The frozen release excludes the later token-usage changes even though those remain in the shared checkout.
- Tests: Current combined baseline 40 passed. Frozen source regression 201 passed, 0 failed, 6 conditional skips; deployment/package checks 17 passed; browser/session tests 8 passed. The compiled OAuth/MCP fixture passed, including bounded batch access and approval-before-native-execution using fixture credentials. Typecheck and backend/UI build passed. After promotion, all 389 deployed files matched the verified candidate; SQLite quick_check was ok with the same seven migrations and no active agent task. Local/public health and both correct OAuth discovery endpoints returned 200; anonymous MCP returned the expected 401.
- Compatibility: Existing Shell/PTY and direct file contracts remain. The service recovered in 7324 ms after its controlled restart, and the tunnel PID did not change. The previous dist and an integrity-checked SQLite/configuration backup were retained. Only pending in-memory sessions/approvals follow the existing restart behavior; durable state was not reset.
- Known Risks: The current host connector still returned oauth_token_invalid_grant after deployment and needs user reauthentication and tool refresh. A separate earlier call also hit host-side transport failure; browser-control initialization was unavailable. Real ChatGPT file modification and live Codex inference were not claimed. New token telemetry, cross-platform skips, initial concurrent DELETE-to-WAL startup risk and long-duration reliability remain outside this release; rollback artifacts were retained without deliberately injecting a production failure.
- Next Highest-Leverage Step: Reauthenticate the existing connector without deleting its registration, refresh its tool list, verify one bounded read and then one explicitly user-approved task. Publish and test the separate telemetry work independently; do not rebuild from an unfinished shared checkout to fix host authentication.

## 2026-09-08 - Preserve Codex usage receipts and reduce repeated host work (L3)

- Current State: [F] DevSpace started at 6e170407 with unrelated working-tree changes. Direct project tools did not launch Codex, but thread/tokenUsage/updated notifications were ignored and task records/daemon observations lacked usage. Existing model and permission boundaries were retained.
- Changes: Added optional, validated latest provider-thread usage snapshots, including cached input, optional cache-write input, output and reasoning counters. Connected Codex adapter -> runtime pool -> manager -> existing execution_json -> daemon -> CLI JSON/MCP observations. Added usage documentation and a workflow link. Project-level guidance prefers direct tools for scoped operations and one primary executor for complex work, retaining the existing approved-model policy.
- Root Cause: [F] The adapter had no usage handler, the runtime pool reconstructed callbacks without forwarding usage, and daemon/presentation layers explicitly selected fields. A new manager failure-path test reproduced the pool omission. Independent review found early rejection on receipt-write failure could release tracking while an unguarded provider still ran; a delayed-completion regression failed before correction and passed after deferring that error until the normal turn boundary.
- Impact: Latest observed snapshots are replaced, never summed or differenced as bills. Thread/turn identities filter unrelated events. Failed/interrupted/disconnected runs retain already-observed receipts; continuations without new events retain the original turn/time, and new provider threads clear old snapshots. Model-evidence rejection does not erase provider-reported usage or release the rejected response.
- Tests: Baseline 5 selected files reported 7 pass / 0 fail. Provider fixture tests 7 pass; provider/policy/runtime regression 25 pass; storage/manager/pool/daemon/presentation/bridge/MCP regression 9 pass after the reproduced callback failure was fixed. Final isolated TypeScript compilation succeeded; 11 compiled test files reported 34 pass / 0 fail / 0 skipped in 32.165s. Typecheck and scoped diff review passed. Bindings generated locally by exact Codex CLI 0.153.4 confirmed notification and cacheWriteInputTokens fields without inference. Related guide checks passed; receipts and source hashes remain private. Fixture/model-policy tests are simulated, not real billing evidence.
- Compatibility: Additive optional usage, no schema migration or new database table. Old records remain readable, missing/invalid telemetry never becomes zero, and protected model/readonly behavior remains. Older binaries can discard unknown usage on write; rollback to old code does not guarantee telemetry retention.
- Known Risks: This is latest-observation telemetry, not an immutable per-request ledger or a complete turn/host/account bill. Provider resets and undelivered notifications preclude deriving savings by subtraction. Host usage and applicable prices remain external. Production dist, running server/daemon, private configuration, credentials and tunnel were not changed; real ChatGPT acceptance and monetary/token savings are unmeasured. Parallel source changes remain outside this iteration's regression claims.
- Next Highest-Leverage Step: Promote a reconciled candidate to both server and agent daemon, then verify usage in the real host on an approved task and compare equal-acceptance workflows with both sides' actual usage.

## 2026-09-08 - Migration history guard and legacy tool mode (L3)

- Current State: The branch started at 17d46cf with 18 unrelated working-tree changes. Database startup identified migrations by version only, legacy tool_mode was rejected, and failed database initialization did not close its connection. The published source and parallel work were kept separate.
- Changes: Adapted the migration version/name guard and legacy tool_mode mapping from upstream PR #309. Preserved personal migration 7 and existing interrupted-upgrade behavior. Added database initialization cleanup, regression coverage and upgrade/recovery documentation; no new migration, state store or retry layer.
- Root Cause: Version numbers alone did not distinguish incompatible branch histories; the supported old tool_mode spelling was absent from validation and conversion. The database connection was allocated before initialization without a failure cleanup path. Failing regressions reproduced all three before the production edits.
- Impact: Unknown migration versions or mismatched names now reject before pending migration writes. Valid v1-v7 state remains compatible. Explicit tools.mode wins over the old spelling, invalid values still fail, and legacy-file backups and Owner credentials are preserved. Only the selected patch was prepared for publication; parallel feature changes and their log entry were excluded.
- Tests: Relevant baseline 5 passed. Focused regression 14 passed. An isolated candidate containing only this patch passed 174 source tests with 0 failures and 6 conditional skips in 334.304 seconds; deployment/package checks 17 passed and explicit browser/login tests 8 passed. Existing-WAL concurrent upgrade passed 5 repeated runs. Typecheck, backend/UI build, compiled legacy conversion and v7 open/reopen checks passed. Tests verify rollback without schema/journal changes, backup/reopen payload preservation, invalid history and failed-open handle release. Diff review passed.
- Compatibility: No public API or database format change; personal execution_json migration remains version 7. The new rejection of inconsistent history is intentional and does not repair or delete state. The new alias is accepted only for legacy config.json, not modern JSONC. Existing permissions, fixed login lifetime, model routing and service configuration were not changed.
- Known Risks: Initial concurrent DELETE-to-WAL conversion produced SQLITE_BUSY in a Windows fixture; this pre-existing startup path was not hidden with retry patches and is documented separately. The concurrency acceptance here concerns an existing WAL database. Non-Windows skipped cases, live ChatGPT acceptance and long-duration performance were not revalidated. This iteration did not replace the running service or touch its database: the running deployment also contains independent unpublished features. Hosted CI eligibility has previously been blocked by account billing and must be checked independently.
- Next Highest-Leverage Step: Introduce isolated QA configuration/state under a separate L3 change, preserving private credentials and explicit execution boundaries. Reconcile the parallel source/deployment changes and verify a combined candidate before promoting these guards to the running service.

## 2026-09-08 - Native WebCodex capability adoption (L3 execution, L2 project access)

- Current State: The user requested folding WebCodex strengths into DevSpace itself. The checkout initially was clean at 17d46cff3afb22687128e6574a1e38af833b683f. Existing workspace identity, paginated reads/search, guarded patches, process sessions and Owner authorization were retained. Later unrelated user-config and database-migration test edits were observed and preserved; they are not owned by this change.
- Changes: Added project_read_batch with 1–8 ordered UTF-16/SHA-256 reads, per-item errors, explicit continuation and an actual serialized UTF-8 result budget; the CLI also accepts read-batch request files. Added native run_process, process_status and process_cancel in both host tool modes, backed by the existing ProcessSessionManager. Added explicit refreshContext for conversation-bound checkout context and current skill catalogs, preserving default deduplication. Updated the development workflow and added docs/webcodex-adoption.md with upstream design attribution. No WebCodex runtime dependency or Rust source copy was introduced.
- Root Cause: Single-file reads required repeated round trips; concatenation alone would not bound batch output. Shell-only execution could not preserve literal argv or provide initial stdin EOF and a separate runtime deadline. Reused checkout bootstrap omitted changed rules. Independent review found that old write_stdin could discard native outcome metadata; it now rejects native sessions before consuming evidence. A real OAuth/MCP regression also exposed root-denial exceptions surfacing as HTTP 500; they now return a safe WORKSPACE_ACCESS_DENIED tool failure without widening access.
- Impact: Batch reads prevalidate all paths, then reuse the existing reader. Authorization examines every item, so a protected file still requires Owner approval. Native execution/cancellation remain high risk, status polling is read-only, and literal argv is not an OS sandbox. Initial stdin is byte-bounded and closed; UTF-8 streaming, actual exit, spawn/stdin error, timeout, cancellation and truncation are separately reported. Refresh cannot create a worktree. No OAuth, model policy, allowed roots, database schema or tunnel configuration changed.
- Tests: Relevant baseline 33 passed / 1 skipped. Batch plus single-read/CLI regressions 14 passed; native process/MCP plus old process/platform regressions 20 passed. The final complete source command reported 192 passed / 6 skipped / 0 failed in 389.157s; skips were three unenabled browser cases and three platform cases. Deployment/package privacy tests 17 passed. Typecheck and an isolated production/UI build passed, with the existing large UI chunk warning retained. A compiled-candidate real OAuth/MCP regression passed, including batch protected-path denial, native pre-approval zero side effects and one exact approved execution using fixture credentials. No production Owner decision was simulated.
- Tests: Seventy-two deployed JavaScript files matched a fresh compile of the final production sources. Source fingerprint ee1e335c71ddb257290dd5b7fbe06289b7f56bbf53aef58f715183e85a7d1c00 matched the deployment manifest. Post-deploy CLI read-batch read two public files in an existing registered project, returned matching SHA-256 values and a 3,233-byte result under an 8,192-byte budget. A first CLI attempt from an unregistered source root was rejected; no roots were expanded. Local health returned 200, anonymous MCP returned 401. Four changed guides had 54 local links and no errors; the final log receives a subsequent scoped check. Receipts are under .runtime/webcodex-native-20260908/ and are not published.
- Compatibility: Existing shell/PTY input and response shapes remain. Native sessions require their own status/cancel tools so old formatters cannot lose execution evidence. The managed local server was updated with a guarded dist backup and recovered in 8,339ms; its tunnel PID and configuration/auth file hashes stayed unchanged. Sources and built output were reread and checked. No commit, push, dependency install or upstream version change was performed.
- Known Risks: Native process sessions are in memory, not durable Jobs; service restart does not recover them, and final reads consume completed sessions. No workflow execution ledger, test-discovery assertion, automatic replay, remote Runner fleet or tunnel manager was added. Windows execution was tested; POSIX process-group behavior and browser UI interaction were not newly verified. The real ChatGPT connector returned oauth_token_invalid_grant before deployment and requires reauthentication; live host discovery/calling of the new tools is still unverified. Broad source tests only cover the files discovered by that run, not concurrent tests added later. After deployment, another task changed config-migration.ts, db/client.ts and db/migrations.ts. The final guard detected that the broader working-tree fingerprint had moved ahead of the deployed snapshot; all six checked feature/authorization/workspace source hashes still matched this iteration. Those parallel production changes were preserved and were not rebuilt, tested or promoted as part of this rollout.
- Next Highest-Leverage Step: Reauthenticate and refresh the existing ChatGPT connector, then verify project_read_batch, run_process/process_status/process_cancel and explicit context refresh through that host. Design persistent execution receipts/restart semantics separately before claiming durable cross-restart jobs.

## 2026-09-08 - Publication and configuration hygiene (L3 rollout, L1 follow-up)

- Current State: The preceding approval and fixed-login changes were deployed but uncommitted. The personal branch started at 11f3d6a with 26 modified files; unrelated local state was preserved.
- Changes: Published the verified feature snapshot as 219dd19. Removed an unrelated one-second startup race from the deploy exit-case test without weakening its failure assertion or the separate timeout test. Corrected the default/generated/documented configuration schema URL to the personal repository, preserved custom schema metadata, and removed the redundant samePolicy forwarding wrapper in favor of the existing comparator. Updated only the local schema metadata and promoted a separately built candidate with rollback backups.
- Root Cause: Fork-specific settings had outgrown the upstream schema, while generated defaults still pointed to it. A new regression assertion reproduced that mismatch. The deployment test conflated early child exit with a tight readiness deadline under scheduling load; production startup behavior was unchanged.
- Impact: Editor validation and new configuration defaults now reference the personal contract. Agent policy comparisons are unchanged. Active MCP session tracking, Owner-page fallback and legacy OAuth compatibility remain because current consumers still use them. No broad module or runtime-state deletion was performed.
- Tests: Pre-publication source regression 165 passed, 0 failed, 6 skipped; explicit browser/session checks 8 passed. Deployment/package checks initially had 1 timing failure, then 17 passed after the isolated test correction. Package dry-run inspected 418 files without private state. Follow-up configuration/onboarding checks 11 passed, policy checks 8 passed and final targeted checks 9 passed. Typecheck, staged builds, generated-schema and diff checks passed. Local/public health passed after promotion, deployed build fingerprint matched source, and protected configuration comparison found only the intended schema metadata change.
- Compatibility: No API, database, approval or execution-policy contract was removed. Custom schema URLs remain supported. Passwords, fixed login lifetime, trusted clients, workspace roots, model policy and supervisor settings were preserved. The earlier opt-in login cutover still requires old connections to reauthenticate once.
- Known Risks: This was a scoped legacy review, not a whole-repository dead-code audit. The first publication reached GitHub, but hosted CI jobs did not start because the account was locked due to a billing issue. Real ChatGPT human approval/inference, reboot and long-duration reliability were not revalidated. Existing skipped platform cases and large UI bundle warnings remain. Source tests were not rerun in full after the small follow-up; the relevant suites were rerun instead.
- Next Highest-Leverage Step: Restore hosted CI eligibility, then validate one bounded human-approved task through the actual ChatGPT card and verify its provider receipt, file diff and test result before expanding provider-internal automatic approval.

## 2026-09-08 - Risk profile and fixed Owner login lifetime (L3)

- Current State: Per-operation authorization used the conservative profile; OAuth refresh extended its lifetime on rotation and no reusable Owner browser login existed. Prior uncommitted UI work was preserved.
- Changes: Added opt-in high_risk_only for ordinary file changes and workspace creation, while retaining sensitive/destructive/broad-execution gates. Added a configurable fixed Owner verification lifetime, signed HttpOnly browser sessions and CSRF-bound connection consent. Access/refresh tokens retain the verified login timestamp as part of their opaque hashed credential, and cannot slide the deadline on refresh or restart. No database migration or password reset.
- Root Cause: Login identity freshness was not represented in token rotation, and the conservative profile also required confirmation for ordinary unguarded file changes.
- Impact: The local deployment now uses high_risk_only and a 43200-second Owner lifetime. Browser identity reuse does not approve operations. Old tokens require one fresh login because their verification time is unknown; client registrations and credentials remain intact.
- Tests: Baseline 9 passed; broad regression 165 passed, 0 failed, 6 skipped; final focused checks 11 passed. Explicit browser/auth tests 12 passed, including a second high-risk decision without re-entering the password. Clock-advanced tests verified exact 12-hour expiry, refresh/restart behavior, tamper rejection, CSRF binding and legacy-token cutover. Typecheck, build, schema and diff checks passed. Local/public health and OAuth discovery returned 200; the live old connection returned UNAUTHORIZED / reauthentication required as expected.
- Compatibility: Defaults retain conservative approval and the old login policy unless explicitly configured. Existing token database format is unchanged. Cutover requires fresh login; rollback requires restoring the previous configuration and build. Credentials, roots, model policy, trusted clients and supervisor settings were preserved.
- Known Risks: This is not complete action-by-action approval inside Codex. Arbitrary shell/process input, sensitive aggregate diffs and unconstrained Codex delegation still require approval because their effects cannot be reliably inferred from a task label. Host confirmations are independent. New real login and Chat UI acceptance require the user; no production credential was entered by the assistant. No commit/push this iteration.
- Next Highest-Leverage Step: Reauthenticate the existing connection once, then verify ordinary direct-file work is automatic. Separately implement bounded provider-internal approval routing before claiming routine Codex programming can run entirely without turn-level consent.

## 2026-09-08 - Chat web approval hardening (L3)

- Current State: Inline approval work was deployed but remained uncommitted. The current host still exposed an older tool snapshot; unrelated earlier changes were preserved.
- Changes: Require a non-empty conversation context for card decisions, revalidate pending scope before review/approval, validate capabilities before filesystem checks, expose explicit card/Owner fallback modes and safe rejection codes. Added standard-first ChatGPT web bridge support, no cross-bridge retries, receipt matching, manual notification recovery, theme-aware compatibility rendering and built-entrypoint browser coverage.
- Root Cause: Missing conversation metadata used a shared null principal component; freshness was only checked at execution rather than consent; the card depended on one UI bridge and lacked a history/notification recovery action. The null-context behavior was reproduced with a failing HTTP regression before the fix.
- Impact: Per-operation confirmation remains single-use. Missing context falls back to the Owner page rather than weakening scope. Restored cards cannot auto-notify, and recovery queries a current receipt without resubmitting a decision. Credentials, configured clients, roots, models and tunnel/supervisor settings were not changed.
- Tests: Baseline 14 passed. Broad regression 161 passed, 0 failed, 6 skipped; final focused checks 16 passed. Four explicit browser cases passed, including the actual bundled entrypoint with only the documented ChatGPT bridge available. Deployment/package tests 17 passed; final typecheck, build and diff checks passed. Local/public health, native-plugin card-mode/conversation-bound response and exact fixture hash read passed. Tests use inert UI/provider fixtures, not production user approvals.
- Compatibility: Existing tool names and inputs remain. Security tightening intentionally rejects unscoped card requests; the Owner page remains compatible. Standard MCP Apps is preferred, with feature-selected ChatGPT aliases and no delivery-failure fallback. Metadata refresh is still needed in the real host.
- Known Risks: Live ChatGPT rendering/click-through and model execution remain unverified; browser control failed to initialize. Raw trusted hosts can access component metadata and remain inside the documented trust boundary. Platform/Pi test skips, existing large UI chunks, reboot and long-term operation remain outside this verification. No commit, push or hosted CI run this iteration.
- Next Highest-Leverage Step: Refresh the current ChatGPT connection, verify review_approval is available to the model while decide_approval remains UI-only, then accept one user-approved bounded task end to end.

## 2026-09-08 - Inline user approval cards (L3)

- Current State: Owner approval required password entry on an external page for each high-risk request. The existing Codex one-turn dispatch and ordinary-file rules were working; the checkout was clean at the start.
- Changes: Added an opt-in trusted OAuth client list, a model-visible review tool and an app-only decision tool. Reused the existing approval state machine and workspace card resource. Added responsive approve/deny/status UI, private-metadata restoration, expiry handling and bounded receipt polling before a single host follow-up. No task/session blanket grants were introduced.
- Root Cause: Identity verification and per-operation consent shared only the external password form; the plugin lacked a component-bound approval entrypoint. An absent-UI-API regression was captured before implementation.
- Impact: Approved clients can use per-operation Chat UI decisions without sending Owner credentials to the model. Private capabilities remain in result metadata and are checked against the original OAuth principal, conversation, request and expiry. Other clients retain the Owner page. Ordinary operations retain their prior classification.
- Tests: Baseline 15 passed. Targeted tests 17 passed. Full source regression 158 passed, 0 failed, 6 skipped; the three browser cases were separately enabled and passed, followed by a passing final UI receipt-timing case. Deployment/package tests 17 passed; typecheck, build, schema and diff checks passed. Inspected 360px/900px browser screenshots. Local/public health and the native plugin's authenticated enabled-policy response passed; the high-risk probe itself did not execute.
- Compatibility: Empty client list preserves prior behavior. Only the authenticated current ChatGPT client was enabled locally. Credentials, roots, model policy and supervisor settings were preserved. The UI requires the host to refresh tool metadata; old clients retain the external-page fallback. Rolling back to an older build also requires restoring the previous config without the new field.
- Known Risks: The selected host must keep result metadata separate from the model; raw OAuth clients capable of reading that metadata are inside this trust boundary. Real ChatGPT card rendering and a human approval remain unverified because the current host caches the old tool list and browser-control initialization failed. No new live inference, arbitrary shell or production approval was performed. Platform/Pi limitations and existing large UI chunks remain. No commit/push or hosted CI run in this iteration.
- Next Highest-Leverage Step: Refresh the existing developer-mode connection, confirm review_approval is available, and perform one real human-approved bounded task from the card.

## 2026-09-08 - Owner-approved Codex submission (L3)

- Current State: The Owner form only issued a grant. Codex work remained unsubmitted until the host retried the request. Related authorization, routing and deployment protections were present locally but not yet published.
- Changes: Owner approval now submits one reviewed Codex start/continue operation, then redirects to a status page. Exact legacy retries return the scoped receipt, not another execution. Shared schema validation and existing durable bridge deduplication are reused. Added browser, lifecycle and authenticated MCP regression coverage and updated usage documentation.
- Root Cause: The approval state transition was not connected to the existing Codex submission path. A regression test reproduced zero submissions after a valid decision before the fix.
- Impact: Only Owner-gated Codex start/continue automatically dispatch. Original OAuth validity and workspace/model context are rechecked. Shell and other high-risk operations retain their exact-retry flow. No autonomous chat loop or permission expansion was added.
- Tests: Relevant baseline 9 passed. Full source regression 156 passed, 0 failed, 5 skipped; the two browser cases were separately enabled with installed Chrome and both passed. Deployment/package tests 17 passed. Typecheck, candidate build, deployed health/discovery and native-plugin hash read passed. Unit/MCP dispatch tests use a fixture provider; browser tests use inert callbacks, not production Owner credentials.
- Compatibility: Tool names, inputs, legacy authorization mode and provider sandbox limits are retained. Owner-gated Codex approval intentionally changes from grant-only to submit-on-approval, visibly disclosed before confirmation. Older host retries receive a receipt. Pending grants expire on restart; durable tasks do not replay.
- Known Risks: A stopped Chat host cannot be automatically awakened. Final results still need task-status retrieval. New live human-approved model execution, reboot, non-Chromium browsers and long-duration reliability were not exercised for this version. Platform-specific cases remained skipped; Pi sandbox integration self-skipped for missing platform dependencies. Build warnings about large UI chunks remain. Hosted CI must be checked independently of local tests.
- Next Highest-Leverage Step: Validate a separately scoped human-approved write-and-test task through the real host, checking exact runtime model, sandbox, file diff and test output.


## 2026-09-09 10:23-10:29 Post-fix functional and network recheck

- Current State: The tested network route and OAuth source/runtime hashes were unchanged; managed server and tunnel processes remained stable during this scoped check.
- Changes: No production code/configuration change or service restart. Created only an isolated read/write fixture and private verification logs.
- Tests: 13 related authentication tests passed with real Chromium, zero failures/skips. Five rounds of four network probes produced 20/20 expected responses; public DevSpace via TUN ranged 0.692-1.321s and via explicit proxy 0.684-1.183s. The unauthenticated Codex endpoint HTTP426 only proves endpoint reachability. Current Codex remote sockets remained Established via TUN.
- MCP acceptance: Complete hash-verified reading, scoped paginated discovery and literal search passed. Fixture create/update previews and actual writes passed; an intentional stale-hash write was rejected and readback preserved the expected content. This INVALID_ARGUMENT is a passing protection test. Private project structure and file identities are omitted.
- Root Cause / Known Risks: A real remaining transport issue occurred at 10:27:22: QUIC connections 0, 2 and 3 simultaneously reported no-recent-network-activity timeouts, with six MCP stream error records. All three re-registered at 10:27:34-35, restoring four HA connections. This is separate from earlier remote-cancel/code0 log entries. Current functional calls succeeded, but uninterrupted network reliability is not established; exact network/TUN/proxy contribution remains unproven.
- Compatibility: Authentication and high-risk approval safeguards passed; production configuration and source files remained unchanged. Test browser processes and temporary test profiles were cleaned up.
- Evidence: Authentication regressions, cleanup/hash checks, network probes, tunnel cancellation records and the summary are retained in private verification logs, outside the published artifact.
- Next Highest-Leverage Step: Isolate cloudflared UDP/QUIC stability under the current TUN route and compare physical-direct or HTTP2 transport before selecting a persistent network adjustment. This turn was verification, not a protocol switch.


## 2026-09-09 11:03 onward - Continued TUN transport diagnosis

- Current State: The tested network route remained enabled. Local health was HTTP200, while public health returned HTTP530/Cloudflare1033. The managed service and tunnel processes were unchanged; no restart was performed in this continuation.
- Changes: Diagnostic artifacts only. No production route, proxy, tunnel protocol, trust store, OAuth setting or service change. A closed-port precondition stopped the planned cloudflared diag invocation before it launched, so no production runtime/config dump was collected.
- Root Cause: The current TUN path is incompatible with the tested Cloudflare edge transport. Four identical edge IPv4 targets on TCP7844 all failed TLS with EOF via the default TUN route. Using the same verified TLS settings, all four passed via a WLAN-bound socket and all four passed with explicit WLAN interface selection. This localizes the failure to the routing/proxy path, but does not separately establish which TUN-core or upstream-proxy restriction causes it.
- Tests: 12 verified TLS comparison cases: default TUN0/4, physical WLAN bind4/4, physical WLAN interface4/4. Hostname/certificate checking remained enabled, with the official cloudflared2026.8.3 embedded public CA bundle loaded only into temporary probe SSLContexts. Earlier default-Python-CA errors were not counted as successes. Live cloudflared QUIC attempts continued timing out; HTTP2 alone is not validated as a fix on the failing TUN route.
- Impact: Public DevSpace/MCP is currently unavailable on the observed route; Codex remote uses its separate working TUN/ChatGPT path. The gauge ha_connections may include dialing work in this cloudflared version, so gauge counts alone are not treated as healthy registrations.
- Compatibility / Recovery: Existing production configurations and services are unchanged. Prepared route-exclusion text contains two loopback CIDRs plus20 official edgeIPv4 and20 edgeIPv6 addresses; fresh DNS returned20 edgeIPv4 addresses with none missing from the list. It excludes no ChatGPT service address.
- Known Risks: Corrected real tunnel registration and authenticated MCP must still be tested after the user applies the exclusions in elevated v2rayN. Historical QUIC success can inhibit fallback according to the version's source; this is a source-supported explanation, not runtime function tracing. The older aggregate-diff approval was not executed.
- Next Highest-Leverage Step: Review the separately prepared route exclusions, then inspect the resulting routes and verify live tunnel/MCP recovery with sustained probes. Detailed configuration and diagnostic evidence remain private.

# Development Log

## 2026-09-09 - Refresh repository contracts and bound workspace state (L3)

- Current State: The personal branch started at 651434a. Workspace objects and physical-root anchors lived in separate unbounded maps. Public configuration, tool and agent examples lacked automated alignment checks. Concurrent controller-accounting changes were present and excluded from this tested publication.
- Changes: Unified workspace objects and anchors into one cache entry, with a 32-entry LRU for stores supporting durable anchors. Persist anchors before exposing a new cache entry. Added cache safety tests, a documentation checker and CI gate, configuration/example/tool-reference contract tests, a documentation index and maintenance roadmap; refreshed architecture, configuration, security and setup guides. Read-only agent examples now declare writeMode: read_only, and Codex examples use an approved model. Unified Chromium endpoint readiness in the existing test helper.
- Root Cause: Separate maps could diverge during cleanup, and objects accumulated without an eviction contract. Failed anchor persistence could still leave a usable cached workspace. Read-only prose did not enforce profile permissions. Browser regressions reproduced a Windows EBUSY race because file existence was treated as completed endpoint publication; the bounded helper retries only startup reads, not approvals or browser actions.
- Impact: Cache hits update recency; eviction retains persistent identity and the original root anchor. Restoration revalidates that anchor and conservatively resets skill activation as a restart does. Legacy/no-store registries retain instance-local behavior rather than evicting identities they cannot safely restore. Documentation checks cover local targets in root Markdown, docs, examples and skills; they do not validate external availability or every command example. No extra runtime manager or parallel state store was introduced.
- Tests: Selected baseline 38 passed / 3 skipped. Final isolated source regression 227 passed / 7 skipped / 0 failed; explicit browser/login checks 9 passed, compiled cache contracts 6 passed, deployment/package checks 17 passed, and documentation-checker tests 5 passed. The documentation scan checked 33 documents and 107 local links with no errors. Typecheck and backend/UI builds passed. Earlier cache failure evidence and the browser EBUSY failure are retained privately; the corrected browser group passed after the helper fix. The first agent-example test attempt failed because its fixture disabled subagents; that setup error is not product-failure evidence. Reading the original example frontmatter separately confirmed the missing permission fields.
- Compatibility: No public MCP input, OAuth, database schema, configured approval lifetime, login freshness, tunnel or active service changes. Cache eviction may require rereading an advertised skill, as documented. Sample profiles do not replace installed user profiles. No package rename/version bump, dependency upgrade, upstream merge, service deployment or history rewrite. Shared unrelated edits remain outside this release.
- Known Risks: Seven conditional/platform cases remain skipped in the general suite; separate Chromium checks cover the browser cases, not every engine or OS. The existing UI chunk-size warning remains. This is repository/fixture acceptance, not new real ChatGPT account, human approval or Codex inference acceptance. Cache size does not bound all process memory or persistent records. Hosted CI eligibility must be verified separately. Protocol migration, durable command receipts, storage lifecycle, repeated project I/O and duplicated version metadata remain explicitly tracked rather than hidden by a broad rewrite.
- Next Highest-Leverage Step: Reconcile the completed parallel changes into a separately tested deployment candidate, then verify the real host read/guarded-edit/approval path against its recorded build. Design durable execution receipts and restart recovery as a separate compatible change before claiming recoverable jobs.

## 2026-09-09 - Publish pending usage and OAuth callback updates (L3 changes, scoped release)

- Current State: The personal branch started at 9f8b22e with 22 pending source, test and documentation files. Usage telemetry and a scoped OAuth callback repair had separate earlier validation records; some local notes contained machine/project identifiers.
- Changes: Prepared the pending usage pipeline and OAuth callback fix for separate commits. Removed local project names, paths, exact project-file fingerprints and process identifiers from the pending public log while preserving private evidence. Added the OAuth redirect browser regression to the existing Windows CI browser step; all three CI platforms remain configured.
- Root Cause: Provider usage notifications were not carried through the execution/persistence/presentation chain, and the OAuth form-action policy did not permit its validated cross-origin callback. The new browser test also needed an explicit CI invocation instead of remaining conditionally skipped. These fixes already existed locally and are now released together.
- Impact: Optional provider-thread usage is retained without converting absent telemetry to zero or claiming billing savings. The callback fix permits only the validated current callback origin and keeps the existing OAuth checks. This publication did not restart services, deploy a new dist, alter credentials or change approval/model policies.
- Tests: Current selected baseline 18 passed. Frozen source regression 216 passed, 0 failed, 7 conditional skips; explicit browser/session checks 9 passed and deployment/package checks 17 passed. Typecheck and backend/UI build passed. The workflow parsed successfully, named real test files and retained the three-platform matrix. Scoped privacy scanning and diff review passed. Historical per-feature acceptance is recorded in its dated entries, not treated as new production verification.
- Compatibility: Additive optional usage uses existing execution_json metadata with no database migration. Old records remain readable; older writers may discard telemetry on downgrade. The 30-120 minute approval policy and 12-hour login configuration are not changed by this release. No upstream merge, package version bump, force-push or history rewrite.
- Known Risks: Full-suite conditional/platform skips and the existing UI chunk warning remain. New live model usage, complete host/account billing and long-duration network behavior were not verified by this publication. Hosted CI availability must be checked separately; local tests cannot substitute for an unstarted remote job. Original local diagnosis details are retained only in private evidence.
- Next Highest-Leverage Step: When deployment is requested, reconcile the server and agent daemon on one tested version, then validate observed usage through a user-authorized task. Restore hosted CI eligibility independently of code publication.

## 2026-09-09 - Repair OAuth browser callback blocked by CSP (L3)

- Current State: Public health was reachable, but reauthentication repeatedly returned an authorization 302 while the browser remained on the consent page; the existing connector reported oauth_token_invalid_grant.
- Changes: Allow only the SDK-validated current callback origin alongside self in the OAuth form-action CSP. Preserve all other CSP directives, Owner verification, PKCE, scope/resource checks, expiry and permission gates. Add src/oauth-redirect.browser.test.ts with an isolated real Chromium form submission and token exchange.
- Root Cause: The prior form-action self policy blocked the successful POST/302 redirect to the registered cross-origin OAuth callback. Two pre-fix Chromium runs each observed an issued authorization code, an explicit CSP violation and no callback request. Mock-response tests did not execute browser navigation.
- Impact: Only src/oauth-provider.ts and its compiled dist/oauth-provider.js change product behavior. Only that compiled file was deployed; unrelated shared work was excluded. The managed service restarted while its tunnel and other processes continued.
- Tests: Existing authentication baseline 10/10 passed. New browser regression failed twice before the fix. After the fix, the browser plus existing authentication regressions passed 11/11 with zero skips; callback GET/state/no-Referer, PKCE token exchange HTTP 200 and token verification passed, while an unrelated origin received zero form submissions. Typecheck and staged TypeScript build both exited 0; independent diff review found no blocking issue.
- Live acceptance: The scoped check recorded browser consent returning to host plugin settings, OAuth token exchange HTTP 200 and a complete paginated read matching its local SHA-256. Private project names, paths, file fingerprints and unrelated application details are omitted. These dated observations do not establish long-duration reliability.
- Compatibility: No OAuth database migration, credential reset, permission expansion, node/TUN change or account/session lifetime change. Ordinary reauthentication remains subject to the existing Owner freshness policy.
- Known Risks: Browser navigation regression was run in installed Chromium, not every browser engine. This is scoped OAuth and current TUN coexistence acceptance, not a full repository regression or long-duration network guarantee. The first File.Replace attempt failed on a null backup argument before changing the target; hashes were rechecked before successful replacement with an explicit same-volume backup.
- Recovery: Source/runtime backups, before/after tests, build/typecheck logs and the deployment receipt are retained privately. Machine-specific backup paths and artifact identifiers are not published.
- Next Highest-Leverage Step: Include this browser regression in release checks with DEVSPACE_TEST_BROWSER set; no additional user action is required for the current connection.

## 2026-09-09 - Bounded 30-120 minute approval lifetime (L3)

- Current State: Owner approvals and their browser nonce cookies each used a hardcoded five-minute lifetime, with a separate static page label. Owner login freshness was already configured for 12 hours. Unrelated token-usage work remained in the shared checkout.
- Changes: Added tools.approvalTtlSeconds with a 1800-second default and an integer range of 1800-7200 seconds. Shared limits feed configuration validation and the approval store; the server now passes the configured lifetime. The Owner page displays the exact request expiry and its HttpOnly cookie uses that same absolute deadline. Updated the schema and authorization/configuration guides.
- Root Cause: Approval duration was not a configuration contract, and independent cookie/text constants could disagree with an extended server deadline. Five failing regressions captured missing defaults/bounds, short expiry and cookie mismatch before the implementation.
- Impact: Pending and unconsumed approvals keep the deadline from their first request. Reopening, reviewing, retrying or approving does not extend that deadline. Single-use consumption and principal/context checks remain. Submission continues independently of approval expiry; the existing terminal receipt window now follows the configured lifetime without granting execution authority.
- Tests: Baseline 14 passed; focused checks 22 passed. The isolated release candidate passed 209 source tests with 0 failures and 6 conditional skips; 13 compiled-contract, 8 browser/login and 17 deployment/package checks passed. Tests cover 30/60/120-minute values, out-of-range/non-integer input, exact expiry, no sliding renewal, late single-use consent, cookie/page consistency and ongoing submission. Typecheck, build, schema generation and diff review passed. After promotion, all 389 deployed files matched; effective approval lifetime was 1800 seconds and Owner login remained 43200. Local/public health and OAuth discovery were 200, anonymous MCP was 401.
- Compatibility: Existing configuration files remain valid and need no edit; the Owner-gated default intentionally changes from five to thirty minutes as requested. The public tool inputs, OAuth credentials, allowed roots, model policy and task runtime/other-provider idle timeouts are unchanged. The separate receipt-retention phase is documented. Restart still invalidates in-memory approvals; no persistence format or migration was added.
- Known Risks: Six conditional/platform cases remained skipped; no new real ChatGPT human approval, production command or Codex inference was performed. A longer window retains approval records longer under the existing per-principal/global limits. This is not a blanket grant, automatic renewal or a two-hour task runtime cap. Old links were invalidated by the controlled restart. Existing large UI chunks and the separate concurrent-WAL startup limitation remain.
- Next Highest-Leverage Step: Use a newly created approval in the real host to confirm the displayed deadline and a user decision. Keep optional future lifetime changes within 1800-7200 seconds; do not lengthen OAuth login freshness or remove approval boundaries to work around host reauthentication.

## 2026-09-08 - Unified native tools and migration guard rollout (L3)

- Current State: Published migration guards and the running native-tool build came from different snapshots. The host still exposed an older tool list and required reauthentication. New token-usage work appeared during preparation and was preserved outside this release.
- Changes: Consolidated the existing batch reader, native process/status/cancel tools and explicit context refresh with the published migration-history and legacy-config fixes. Prepared one frozen source snapshot, backed up the current configuration and SQLite state, then promoted its verified dist through the existing supervisor. No permission, login-lifetime, model-policy, tunnel or database-format expansion.
- Root Cause: Source publication and runtime promotion had occurred in separate work streams; a cached host tool list and invalid OAuth grant were independent of backend health. Rebuilding cannot silently restore a host's authorization, so no credentials were reset or approvals bypassed.
- Impact: Runtime now retains the native-tool capabilities while also enforcing migration version/name checks and failed-open cleanup. The fixed 12-hour Owner policy and all six protected configuration files were preserved. The frozen release excludes the later token-usage changes even though those remain in the shared checkout.
- Tests: Current combined baseline 40 passed. Frozen source regression 201 passed, 0 failed, 6 conditional skips; deployment/package checks 17 passed; browser/session tests 8 passed. The compiled OAuth/MCP fixture passed, including bounded batch access and approval-before-native-execution using fixture credentials. Typecheck and backend/UI build passed. After promotion, all 389 deployed files matched the verified candidate; SQLite quick_check was ok with the same seven migrations and no active agent task. Local/public health and both correct OAuth discovery endpoints returned 200; anonymous MCP returned the expected 401.
- Compatibility: Existing Shell/PTY and direct file contracts remain. The service recovered in 7324 ms after its controlled restart, and the tunnel PID did not change. The previous dist and an integrity-checked SQLite/configuration backup were retained. Only pending in-memory sessions/approvals follow the existing restart behavior; durable state was not reset.
- Known Risks: The current host connector still returned oauth_token_invalid_grant after deployment and needs user reauthentication and tool refresh. A separate earlier call also hit host-side transport failure; browser-control initialization was unavailable. Real ChatGPT file modification and live Codex inference were not claimed. New token telemetry, cross-platform skips, initial concurrent DELETE-to-WAL startup risk and long-duration reliability remain outside this release; rollback artifacts were retained without deliberately injecting a production failure.
- Next Highest-Leverage Step: Reauthenticate the existing connector without deleting its registration, refresh its tool list, verify one bounded read and then one explicitly user-approved task. Publish and test the separate telemetry work independently; do not rebuild from an unfinished shared checkout to fix host authentication.

## 2026-09-08 - Preserve Codex usage receipts and reduce repeated host work (L3)

- Current State: [F] DevSpace started at 6e170407 with unrelated working-tree changes. Direct project tools did not launch Codex, but thread/tokenUsage/updated notifications were ignored and task records/daemon observations lacked usage. Existing model and permission boundaries were retained.
- Changes: Added optional, validated latest provider-thread usage snapshots, including cached input, optional cache-write input, output and reasoning counters. Connected Codex adapter -> runtime pool -> manager -> existing execution_json -> daemon -> CLI JSON/MCP observations. Added usage documentation and a workflow link. Project-level guidance prefers direct tools for scoped operations and one primary executor for complex work, retaining the existing approved-model policy.
- Root Cause: [F] The adapter had no usage handler, the runtime pool reconstructed callbacks without forwarding usage, and daemon/presentation layers explicitly selected fields. A new manager failure-path test reproduced the pool omission. Independent review found early rejection on receipt-write failure could release tracking while an unguarded provider still ran; a delayed-completion regression failed before correction and passed after deferring that error until the normal turn boundary.
- Impact: Latest observed snapshots are replaced, never summed or differenced as bills. Thread/turn identities filter unrelated events. Failed/interrupted/disconnected runs retain already-observed receipts; continuations without new events retain the original turn/time, and new provider threads clear old snapshots. Model-evidence rejection does not erase provider-reported usage or release the rejected response.
- Tests: Baseline 5 selected files reported 7 pass / 0 fail. Provider fixture tests 7 pass; provider/policy/runtime regression 25 pass; storage/manager/pool/daemon/presentation/bridge/MCP regression 9 pass after the reproduced callback failure was fixed. Final isolated TypeScript compilation succeeded; 11 compiled test files reported 34 pass / 0 fail / 0 skipped in 32.165s. Typecheck and scoped diff review passed. Bindings generated locally by exact Codex CLI 0.153.4 confirmed notification and cacheWriteInputTokens fields without inference. Related guide checks passed; receipts and source hashes remain private. Fixture/model-policy tests are simulated, not real billing evidence.
- Compatibility: Additive optional usage, no schema migration or new database table. Old records remain readable, missing/invalid telemetry never becomes zero, and protected model/readonly behavior remains. Older binaries can discard unknown usage on write; rollback to old code does not guarantee telemetry retention.
- Known Risks: This is latest-observation telemetry, not an immutable per-request ledger or a complete turn/host/account bill. Provider resets and undelivered notifications preclude deriving savings by subtraction. Host usage and applicable prices remain external. Production dist, running server/daemon, private configuration, credentials and tunnel were not changed; real ChatGPT acceptance and monetary/token savings are unmeasured. Parallel source changes remain outside this iteration's regression claims.
- Next Highest-Leverage Step: Promote a reconciled candidate to both server and agent daemon, then verify usage in the real host on an approved task and compare equal-acceptance workflows with both sides' actual usage.

## 2026-09-08 - Migration history guard and legacy tool mode (L3)

- Current State: The branch started at 17d46cf with 18 unrelated working-tree changes. Database startup identified migrations by version only, legacy tool_mode was rejected, and failed database initialization did not close its connection. The published source and parallel work were kept separate.
- Changes: Adapted the migration version/name guard and legacy tool_mode mapping from upstream PR #309. Preserved personal migration 7 and existing interrupted-upgrade behavior. Added database initialization cleanup, regression coverage and upgrade/recovery documentation; no new migration, state store or retry layer.
- Root Cause: Version numbers alone did not distinguish incompatible branch histories; the supported old tool_mode spelling was absent from validation and conversion. The database connection was allocated before initialization without a failure cleanup path. Failing regressions reproduced all three before the production edits.
- Impact: Unknown migration versions or mismatched names now reject before pending migration writes. Valid v1-v7 state remains compatible. Explicit tools.mode wins over the old spelling, invalid values still fail, and legacy-file backups and Owner credentials are preserved. Only the selected patch was prepared for publication; parallel feature changes and their log entry were excluded.
- Tests: Relevant baseline 5 passed. Focused regression 14 passed. An isolated candidate containing only this patch passed 174 source tests with 0 failures and 6 conditional skips in 334.304 seconds; deployment/package checks 17 passed and explicit browser/login tests 8 passed. Existing-WAL concurrent upgrade passed 5 repeated runs. Typecheck, backend/UI build, compiled legacy conversion and v7 open/reopen checks passed. Tests verify rollback without schema/journal changes, backup/reopen payload preservation, invalid history and failed-open handle release. Diff review passed.
- Compatibility: No public API or database format change; personal execution_json migration remains version 7. The new rejection of inconsistent history is intentional and does not repair or delete state. The new alias is accepted only for legacy config.json, not modern JSONC. Existing permissions, fixed login lifetime, model routing and service configuration were not changed.
- Known Risks: Initial concurrent DELETE-to-WAL conversion produced SQLITE_BUSY in a Windows fixture; this pre-existing startup path was not hidden with retry patches and is documented separately. The concurrency acceptance here concerns an existing WAL database. Non-Windows skipped cases, live ChatGPT acceptance and long-duration performance were not revalidated. This iteration did not replace the running service or touch its database: the running deployment also contains independent unpublished features. Hosted CI eligibility has previously been blocked by account billing and must be checked independently.
- Next Highest-Leverage Step: Introduce isolated QA configuration/state under a separate L3 change, preserving private credentials and explicit execution boundaries. Reconcile the parallel source/deployment changes and verify a combined candidate before promoting these guards to the running service.

## 2026-09-08 - Native WebCodex capability adoption (L3 execution, L2 project access)

- Current State: The user requested folding WebCodex strengths into DevSpace itself. The checkout initially was clean at 17d46cff3afb22687128e6574a1e38af833b683f. Existing workspace identity, paginated reads/search, guarded patches, process sessions and Owner authorization were retained. Later unrelated user-config and database-migration test edits were observed and preserved; they are not owned by this change.
- Changes: Added project_read_batch with 1–8 ordered UTF-16/SHA-256 reads, per-item errors, explicit continuation and an actual serialized UTF-8 result budget; the CLI also accepts read-batch request files. Added native run_process, process_status and process_cancel in both host tool modes, backed by the existing ProcessSessionManager. Added explicit refreshContext for conversation-bound checkout context and current skill catalogs, preserving default deduplication. Updated the development workflow and added docs/webcodex-adoption.md with upstream design attribution. No WebCodex runtime dependency or Rust source copy was introduced.
- Root Cause: Single-file reads required repeated round trips; concatenation alone would not bound batch output. Shell-only execution could not preserve literal argv or provide initial stdin EOF and a separate runtime deadline. Reused checkout bootstrap omitted changed rules. Independent review found that old write_stdin could discard native outcome metadata; it now rejects native sessions before consuming evidence. A real OAuth/MCP regression also exposed root-denial exceptions surfacing as HTTP 500; they now return a safe WORKSPACE_ACCESS_DENIED tool failure without widening access.
- Impact: Batch reads prevalidate all paths, then reuse the existing reader. Authorization examines every item, so a protected file still requires Owner approval. Native execution/cancellation remain high risk, status polling is read-only, and literal argv is not an OS sandbox. Initial stdin is byte-bounded and closed; UTF-8 streaming, actual exit, spawn/stdin error, timeout, cancellation and truncation are separately reported. Refresh cannot create a worktree. No OAuth, model policy, allowed roots, database schema or tunnel configuration changed.
- Tests: Relevant baseline 33 passed / 1 skipped. Batch plus single-read/CLI regressions 14 passed; native process/MCP plus old process/platform regressions 20 passed. The final complete source command reported 192 passed / 6 skipped / 0 failed in 389.157s; skips were three unenabled browser cases and three platform cases. Deployment/package privacy tests 17 passed. Typecheck and an isolated production/UI build passed, with the existing large UI chunk warning retained. A compiled-candidate real OAuth/MCP regression passed, including batch protected-path denial, native pre-approval zero side effects and one exact approved execution using fixture credentials. No production Owner decision was simulated.
- Tests: Seventy-two deployed JavaScript files matched a fresh compile of the final production sources. Source fingerprint ee1e335c71ddb257290dd5b7fbe06289b7f56bbf53aef58f715183e85a7d1c00 matched the deployment manifest. Post-deploy CLI read-batch read two public files in an existing registered project, returned matching SHA-256 values and a 3,233-byte result under an 8,192-byte budget. A first CLI attempt from an unregistered source root was rejected; no roots were expanded. Local health returned 200, anonymous MCP returned 401. Four changed guides had 54 local links and no errors; the final log receives a subsequent scoped check. Receipts are under .runtime/webcodex-native-20260908/ and are not published.
- Compatibility: Existing shell/PTY input and response shapes remain. Native sessions require their own status/cancel tools so old formatters cannot lose execution evidence. The managed local server was updated with a guarded dist backup and recovered in 8,339ms; its tunnel PID and configuration/auth file hashes stayed unchanged. Sources and built output were reread and checked. No commit, push, dependency install or upstream version change was performed.
- Known Risks: Native process sessions are in memory, not durable Jobs; service restart does not recover them, and final reads consume completed sessions. No workflow execution ledger, test-discovery assertion, automatic replay, remote Runner fleet or tunnel manager was added. Windows execution was tested; POSIX process-group behavior and browser UI interaction were not newly verified. The real ChatGPT connector returned oauth_token_invalid_grant before deployment and requires reauthentication; live host discovery/calling of the new tools is still unverified. Broad source tests only cover the files discovered by that run, not concurrent tests added later. After deployment, another task changed config-migration.ts, db/client.ts and db/migrations.ts. The final guard detected that the broader working-tree fingerprint had moved ahead of the deployed snapshot; all six checked feature/authorization/workspace source hashes still matched this iteration. Those parallel production changes were preserved and were not rebuilt, tested or promoted as part of this rollout.
- Next Highest-Leverage Step: Reauthenticate and refresh the existing ChatGPT connector, then verify project_read_batch, run_process/process_status/process_cancel and explicit context refresh through that host. Design persistent execution receipts/restart semantics separately before claiming durable cross-restart jobs.

## 2026-09-08 - Publication and configuration hygiene (L3 rollout, L1 follow-up)

- Current State: The preceding approval and fixed-login changes were deployed but uncommitted. The personal branch started at 11f3d6a with 26 modified files; unrelated local state was preserved.
- Changes: Published the verified feature snapshot as 219dd19. Removed an unrelated one-second startup race from the deploy exit-case test without weakening its failure assertion or the separate timeout test. Corrected the default/generated/documented configuration schema URL to the personal repository, preserved custom schema metadata, and removed the redundant samePolicy forwarding wrapper in favor of the existing comparator. Updated only the local schema metadata and promoted a separately built candidate with rollback backups.
- Root Cause: Fork-specific settings had outgrown the upstream schema, while generated defaults still pointed to it. A new regression assertion reproduced that mismatch. The deployment test conflated early child exit with a tight readiness deadline under scheduling load; production startup behavior was unchanged.
- Impact: Editor validation and new configuration defaults now reference the personal contract. Agent policy comparisons are unchanged. Active MCP session tracking, Owner-page fallback and legacy OAuth compatibility remain because current consumers still use them. No broad module or runtime-state deletion was performed.
- Tests: Pre-publication source regression 165 passed, 0 failed, 6 skipped; explicit browser/session checks 8 passed. Deployment/package checks initially had 1 timing failure, then 17 passed after the isolated test correction. Package dry-run inspected 418 files without private state. Follow-up configuration/onboarding checks 11 passed, policy checks 8 passed and final targeted checks 9 passed. Typecheck, staged builds, generated-schema and diff checks passed. Local/public health passed after promotion, deployed build fingerprint matched source, and protected configuration comparison found only the intended schema metadata change.
- Compatibility: No API, database, approval or execution-policy contract was removed. Custom schema URLs remain supported. Passwords, fixed login lifetime, trusted clients, workspace roots, model policy and supervisor settings were preserved. The earlier opt-in login cutover still requires old connections to reauthenticate once.
- Known Risks: This was a scoped legacy review, not a whole-repository dead-code audit. The first publication reached GitHub, but hosted CI jobs did not start because the account was locked due to a billing issue. Real ChatGPT human approval/inference, reboot and long-duration reliability were not revalidated. Existing skipped platform cases and large UI bundle warnings remain. Source tests were not rerun in full after the small follow-up; the relevant suites were rerun instead.
- Next Highest-Leverage Step: Restore hosted CI eligibility, then validate one bounded human-approved task through the actual ChatGPT card and verify its provider receipt, file diff and test result before expanding provider-internal automatic approval.

## 2026-09-08 - Risk profile and fixed Owner login lifetime (L3)

- Current State: Per-operation authorization used the conservative profile; OAuth refresh extended its lifetime on rotation and no reusable Owner browser login existed. Prior uncommitted UI work was preserved.
- Changes: Added opt-in high_risk_only for ordinary file changes and workspace creation, while retaining sensitive/destructive/broad-execution gates. Added a configurable fixed Owner verification lifetime, signed HttpOnly browser sessions and CSRF-bound connection consent. Access/refresh tokens retain the verified login timestamp as part of their opaque hashed credential, and cannot slide the deadline on refresh or restart. No database migration or password reset.
- Root Cause: Login identity freshness was not represented in token rotation, and the conservative profile also required confirmation for ordinary unguarded file changes.
- Impact: The local deployment now uses high_risk_only and a 43200-second Owner lifetime. Browser identity reuse does not approve operations. Old tokens require one fresh login because their verification time is unknown; client registrations and credentials remain intact.
- Tests: Baseline 9 passed; broad regression 165 passed, 0 failed, 6 skipped; final focused checks 11 passed. Explicit browser/auth tests 12 passed, including a second high-risk decision without re-entering the password. Clock-advanced tests verified exact 12-hour expiry, refresh/restart behavior, tamper rejection, CSRF binding and legacy-token cutover. Typecheck, build, schema and diff checks passed. Local/public health and OAuth discovery returned 200; the live old connection returned UNAUTHORIZED / reauthentication required as expected.
- Compatibility: Defaults retain conservative approval and the old login policy unless explicitly configured. Existing token database format is unchanged. Cutover requires fresh login; rollback requires restoring the previous configuration and build. Credentials, roots, model policy, trusted clients and supervisor settings were preserved.
- Known Risks: This is not complete action-by-action approval inside Codex. Arbitrary shell/process input, sensitive aggregate diffs and unconstrained Codex delegation still require approval because their effects cannot be reliably inferred from a task label. Host confirmations are independent. New real login and Chat UI acceptance require the user; no production credential was entered by the assistant. No commit/push this iteration.
- Next Highest-Leverage Step: Reauthenticate the existing connection once, then verify ordinary direct-file work is automatic. Separately implement bounded provider-internal approval routing before claiming routine Codex programming can run entirely without turn-level consent.

## 2026-09-08 - Chat web approval hardening (L3)

- Current State: Inline approval work was deployed but remained uncommitted. The current host still exposed an older tool snapshot; unrelated earlier changes were preserved.
- Changes: Require a non-empty conversation context for card decisions, revalidate pending scope before review/approval, validate capabilities before filesystem checks, expose explicit card/Owner fallback modes and safe rejection codes. Added standard-first ChatGPT web bridge support, no cross-bridge retries, receipt matching, manual notification recovery, theme-aware compatibility rendering and built-entrypoint browser coverage.
- Root Cause: Missing conversation metadata used a shared null principal component; freshness was only checked at execution rather than consent; the card depended on one UI bridge and lacked a history/notification recovery action. The null-context behavior was reproduced with a failing HTTP regression before the fix.
- Impact: Per-operation confirmation remains single-use. Missing context falls back to the Owner page rather than weakening scope. Restored cards cannot auto-notify, and recovery queries a current receipt without resubmitting a decision. Credentials, configured clients, roots, models and tunnel/supervisor settings were not changed.
- Tests: Baseline 14 passed. Broad regression 161 passed, 0 failed, 6 skipped; final focused checks 16 passed. Four explicit browser cases passed, including the actual bundled entrypoint with only the documented ChatGPT bridge available. Deployment/package tests 17 passed; final typecheck, build and diff checks passed. Local/public health, native-plugin card-mode/conversation-bound response and exact fixture hash read passed. Tests use inert UI/provider fixtures, not production user approvals.
- Compatibility: Existing tool names and inputs remain. Security tightening intentionally rejects unscoped card requests; the Owner page remains compatible. Standard MCP Apps is preferred, with feature-selected ChatGPT aliases and no delivery-failure fallback. Metadata refresh is still needed in the real host.
- Known Risks: Live ChatGPT rendering/click-through and model execution remain unverified; browser control failed to initialize. Raw trusted hosts can access component metadata and remain inside the documented trust boundary. Platform/Pi test skips, existing large UI chunks, reboot and long-term operation remain outside this verification. No commit, push or hosted CI run this iteration.
- Next Highest-Leverage Step: Refresh the current ChatGPT connection, verify review_approval is available to the model while decide_approval remains UI-only, then accept one user-approved bounded task end to end.

## 2026-09-08 - Inline user approval cards (L3)

- Current State: Owner approval required password entry on an external page for each high-risk request. The existing Codex one-turn dispatch and ordinary-file rules were working; the checkout was clean at the start.
- Changes: Added an opt-in trusted OAuth client list, a model-visible review tool and an app-only decision tool. Reused the existing approval state machine and workspace card resource. Added responsive approve/deny/status UI, private-metadata restoration, expiry handling and bounded receipt polling before a single host follow-up. No task/session blanket grants were introduced.
- Root Cause: Identity verification and per-operation consent shared only the external password form; the plugin lacked a component-bound approval entrypoint. An absent-UI-API regression was captured before implementation.
- Impact: Approved clients can use per-operation Chat UI decisions without sending Owner credentials to the model. Private capabilities remain in result metadata and are checked against the original OAuth principal, conversation, request and expiry. Other clients retain the Owner page. Ordinary operations retain their prior classification.
- Tests: Baseline 15 passed. Targeted tests 17 passed. Full source regression 158 passed, 0 failed, 6 skipped; the three browser cases were separately enabled and passed, followed by a passing final UI receipt-timing case. Deployment/package tests 17 passed; typecheck, build, schema and diff checks passed. Inspected 360px/900px browser screenshots. Local/public health and the native plugin's authenticated enabled-policy response passed; the high-risk probe itself did not execute.
- Compatibility: Empty client list preserves prior behavior. Only the authenticated current ChatGPT client was enabled locally. Credentials, roots, model policy and supervisor settings were preserved. The UI requires the host to refresh tool metadata; old clients retain the external-page fallback. Rolling back to an older build also requires restoring the previous config without the new field.
- Known Risks: The selected host must keep result metadata separate from the model; raw OAuth clients capable of reading that metadata are inside this trust boundary. Real ChatGPT card rendering and a human approval remain unverified because the current host caches the old tool list and browser-control initialization failed. No new live inference, arbitrary shell or production approval was performed. Platform/Pi limitations and existing large UI chunks remain. No commit/push or hosted CI run in this iteration.
- Next Highest-Leverage Step: Refresh the existing developer-mode connection, confirm review_approval is available, and perform one real human-approved bounded task from the card.

## 2026-09-08 - Owner-approved Codex submission (L3)

- Current State: The Owner form only issued a grant. Codex work remained unsubmitted until the host retried the request. Related authorization, routing and deployment protections were present locally but not yet published.
- Changes: Owner approval now submits one reviewed Codex start/continue operation, then redirects to a status page. Exact legacy retries return the scoped receipt, not another execution. Shared schema validation and existing durable bridge deduplication are reused. Added browser, lifecycle and authenticated MCP regression coverage and updated usage documentation.
- Root Cause: The approval state transition was not connected to the existing Codex submission path. A regression test reproduced zero submissions after a valid decision before the fix.
- Impact: Only Owner-gated Codex start/continue automatically dispatch. Original OAuth validity and workspace/model context are rechecked. Shell and other high-risk operations retain their exact-retry flow. No autonomous chat loop or permission expansion was added.
- Tests: Relevant baseline 9 passed. Full source regression 156 passed, 0 failed, 5 skipped; the two browser cases were separately enabled with installed Chrome and both passed. Deployment/package tests 17 passed. Typecheck, candidate build, deployed health/discovery and native-plugin hash read passed. Unit/MCP dispatch tests use a fixture provider; browser tests use inert callbacks, not production Owner credentials.
- Compatibility: Tool names, inputs, legacy authorization mode and provider sandbox limits are retained. Owner-gated Codex approval intentionally changes from grant-only to submit-on-approval, visibly disclosed before confirmation. Older host retries receive a receipt. Pending grants expire on restart; durable tasks do not replay.
- Known Risks: A stopped Chat host cannot be automatically awakened. Final results still need task-status retrieval. New live human-approved model execution, reboot, non-Chromium browsers and long-duration reliability were not exercised for this version. Platform-specific cases remained skipped; Pi sandbox integration self-skipped for missing platform dependencies. Build warnings about large UI chunks remain. Hosted CI must be checked independently of local tests.
- Next Highest-Leverage Step: Validate a separately scoped human-approved write-and-test task through the real host, checking exact runtime model, sandbox, file diff and test output.


## 2026-09-09 10:23-10:29 Post-fix functional and network recheck

- Current State: The tested network route and OAuth source/runtime hashes were unchanged; managed server and tunnel processes remained stable during this scoped check.
- Changes: No production code/configuration change or service restart. Created only an isolated read/write fixture and private verification logs.
- Tests: 13 related authentication tests passed with real Chromium, zero failures/skips. Five rounds of four network probes produced 20/20 expected responses; public DevSpace via TUN ranged 0.692-1.321s and via explicit proxy 0.684-1.183s. The unauthenticated Codex endpoint HTTP426 only proves endpoint reachability. Current Codex remote sockets remained Established via TUN.
- MCP acceptance: Complete hash-verified reading, scoped paginated discovery and literal search passed. Fixture create/update previews and actual writes passed; an intentional stale-hash write was rejected and readback preserved the expected content. This INVALID_ARGUMENT is a passing protection test. Private project structure and file identities are omitted.
- Root Cause / Known Risks: A real remaining transport issue occurred at 10:27:22: QUIC connections 0, 2 and 3 simultaneously reported no-recent-network-activity timeouts, with six MCP stream error records. All three re-registered at 10:27:34-35, restoring four HA connections. This is separate from earlier remote-cancel/code0 log entries. Current functional calls succeeded, but uninterrupted network reliability is not established; exact network/TUN/proxy contribution remains unproven.
- Compatibility: Authentication and high-risk approval safeguards passed; production configuration and source files remained unchanged. Test browser processes and temporary test profiles were cleaned up.
- Evidence: Authentication regressions, cleanup/hash checks, network probes, tunnel cancellation records and the summary are retained in private verification logs, outside the published artifact.
- Next Highest-Leverage Step: Isolate cloudflared UDP/QUIC stability under the current TUN route and compare physical-direct or HTTP2 transport before selecting a persistent network adjustment. This turn was verification, not a protocol switch.


## 2026-09-09 11:03 onward - Continued TUN transport diagnosis

- Current State: The tested network route remained enabled. Local health was HTTP200, while public health returned HTTP530/Cloudflare1033. The managed service and tunnel processes were unchanged; no restart was performed in this continuation.
- Changes: Diagnostic artifacts only. No production route, proxy, tunnel protocol, trust store, OAuth setting or service change. A closed-port precondition stopped the planned cloudflared diag invocation before it launched, so no production runtime/config dump was collected.
- Root Cause: The current TUN path is incompatible with the tested Cloudflare edge transport. Four identical edge IPv4 targets on TCP7844 all failed TLS with EOF via the default TUN route. Using the same verified TLS settings, all four passed via a WLAN-bound socket and all four passed with explicit WLAN interface selection. This localizes the failure to the routing/proxy path, but does not separately establish which TUN-core or upstream-proxy restriction causes it.
- Tests: 12 verified TLS comparison cases: default TUN0/4, physical WLAN bind4/4, physical WLAN interface4/4. Hostname/certificate checking remained enabled, with the official cloudflared2026.8.3 embedded public CA bundle loaded only into temporary probe SSLContexts. Earlier default-Python-CA errors were not counted as successes. Live cloudflared QUIC attempts continued timing out; HTTP2 alone is not validated as a fix on the failing TUN route.
- Impact: Public DevSpace/MCP is currently unavailable on the observed route; Codex remote uses its separate working TUN/ChatGPT path. The gauge ha_connections may include dialing work in this cloudflared version, so gauge counts alone are not treated as healthy registrations.
- Compatibility / Recovery: Existing production configurations and services are unchanged. Prepared route-exclusion text contains two loopback CIDRs plus20 official edgeIPv4 and20 edgeIPv6 addresses; fresh DNS returned20 edgeIPv4 addresses with none missing from the list. It excludes no ChatGPT service address.
- Known Risks: Corrected real tunnel registration and authenticated MCP must still be tested after the user applies the exclusions in elevated v2rayN. Historical QUIC success can inhibit fallback according to the version's source; this is a source-supported explanation, not runtime function tracing. The older aggregate-diff approval was not executed.
- Next Highest-Leverage Step: Review the separately prepared route exclusions, then inspect the resulting routes and verify live tunnel/MCP recovery with sustained probes. Detailed configuration and diagnostic evidence remain private.
